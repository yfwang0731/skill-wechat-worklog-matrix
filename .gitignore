# Python 缓存
__pycache__/
*.pyc

# 本地运行时产物（探测/解密/导出中间物，含账号目录与聊天明文，严禁入库）
wechat_pilot/
*.db
all_keys*.json
keys_*.json

# 本地个人配置（含账号目录/路径等占位符，由 config.example.json 复制而来，不入库）
config.json

# 云文档通道中间产物（含真实表格内容/文档 id，不入库）
sheet_snapshot.json
raw_*.json
kdocs_update*.json
payload*.json
merged_preview.csv
final_rows.csv

# 写入水位：`final` 记下本批占用的行区间，用于挡住"拿旧快照重跑"的重复追加
.last_write.json

# 子代理产出：含**聊天原文（evidence）**与真人姓名。它落在 `--out-dir`，
# 而 --out-dir 一旦指到 wechat_pilot/ 之外，原本就没有任何东西兜住它。
# ⚠️ 这个 glob **两头都会伤人**，两种写法都要避开：
#   · **过宽**（如 `agent*.json`）→ 会吞掉 `references/agent-contract.json`（子代理产出契约的
#     **唯一真相源**）⇒ 它进不了仓库：本地有它、自检全绿，CI 与新克隆里都没有，
#     表现是一批检查报"找不到产出契约"。也别写 `agent[-_0-9]*.json` —— 字符组里的 `-` 照样匹配连字符。
#   · **过窄**（如 `agent_*.json`）→ 漏掉**真机形态** `agent<字母>_<账号名>.json`
#     （`_` 必须**紧跟** `agent`，而真机那里是字母）⇒ "含聊天原文的产物别入库"这条兜底
#     **静默失效**；断言只钉一种形态时照样全绿。**漏掉的代价是隐私，比 CI 报错严重得多。**
#   · 现在这个 `agent[!-]*.json` 两头都对：命中 `agentA_<账号>.json` / `agent_1.json` / `agent1.json`，
#     **不**命中紧跟 `agent` 为连字符的 `agent-contract.json`。git wildmatch 与 fnmatch 两侧
#     语义各实测过一遍（自检里另有四种形态的断言钉着，别只测一种）。
agent[!-]*.json

# 这两个默认落在 **cwd**：`pipeline.py probe --dump` 与 `position_reuse --history`。
# 从仓库根目录跑就会直接进仓库 —— README 还手把手教人 dump 出 probe.json。
probe.json
history_positions.json

# 判定规则行为验证的产物（`rules_check.py plan`）
# ⚠ prompt_rendered.txt 是**渲染后的完整提示词** —— 含处理人姓名、**我方标识（微信号）**、
#   对方关键字、服务对象、业务系统描述。这是全流程里最敏感的文件之一。
#   rules_check.py 的默认 --out 就是 <cwd>/rules_check_out，从仓库根目录跑会直接落进仓库。
rules_check_out/
prompt_rendered.txt
rules_cases/

# 产出契约校验报告（`preview` 写在 --out 同目录）—— 含 agent 文件名（真机带人名拼音）与违约值
validation_report.txt

# 编辑器 / 系统文件
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
*.log
