# Changelog

`wechat-worklog-matrix` 的全部重要变更。格式参考 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。**按时间倒序**，日期为 `YYYY-MM-DD`。

> **关于版本号**：**已打 tag 的版本以文末「版本链接」区为准，此处不列举** —— tag 由发布者按发布时机
> 打，正文里写死一份列表，在发布那一刻就必然与事实脱节（而且没有任何机器检查会提醒你）。
> `0.x` 是发布前各开发阶段的**回溯标注**，没有对应 tag，不要据此 checkout。
>
> **关于本文件里的数字**：凡标「实测」的都是真机跑出来的（Windows + 微信 4.1 + WPS 云文档），
> 可用 `scripts/smoke_test.py` 复现。所有条目**均已脱敏** —— 不含真实姓名、微信号、账号目录、
> 客户名、项目编号或群标识。
>
> **本文件不写"当前状态"数字**（当前 N 行 / 当前 N 项 / 当前 N 个标题）—— 它们在下一次改动时
> 必然失真，而**没有任何检查能发现**（真栽过：本文件写了 README「143 行」，随后压掉两行就忘了改）。
> 可以写的是两类：**改动量**（`191 → 141 行`、`替换了 12 处` —— 描述"这次做了什么"，永远成立）
> 与**标注可复现的实测值**。两类都**必须在落笔当次用命令核一遍**。
> 现状数字只允许出现在**有断言守着**的地方（README 的自检项数、CI 格数、夹具条数）。

文末另有「独立评测记录」（历次评分轨迹）与「设计上的不变量」两节。

---

## [1.2.0] - 2026-09-19

> 语义化版本判定：**新增了对外能力** —— `final` / `position_reuse` 新增 `--force`、
> `sheet_snapshot.py inspect` 新增 `--config`、新增 `references/agent-contract.json`；
> 另有若干**用户可感知的行为变更**（起始行非空即拒、零导出非零退出、`--remove` 序号打错报错、
> 待裁决行「状态」留空、合并自动取较早的行、去重判据收紧）。没有产出格式变化、没有配置字段增删
> ⇒ **minor**。**这是本仓库至今最重的一次 minor** —— 三条线的审查与第一次真机全链路验证叠在一起。

本轮把三段工作并进了同一个版本 —— 本节内**不带括号的小节（Added / Changed / Fixed）是"系统性
问题普查"的产出；带括号的小节依次是"真机全链路验证"与"发布那一刻"的产出**。

**业务正确性审查 + 第一次真机全链路验证**。审查查的是"它会产出一份看起来正常的错台账"的那些
路径（三条线：数据链条的语义损失 / 隐私与敏感数据流向 / 判定规则一致性与边界）。真机验证拿真实
数据从头跑到尾 —— 本机微信库 → 解密 → 导出 → 分包 → 3 路并行判定子代理 → `preview` → `final`，
范围限"备注含某关键字的联系人与群聊"，全程只读**台账副本**。
**Fixed（真机全链路跑出来的）** 那一组**只有真跑才会露出来** —— 自检与 CI 在修复前**全是绿的**。

**系统性问题普查**（三个方向并行：代码链路与错误处理 / 文档体系 / CLI 契约与自检盲区），
关键条目都回读源码复核过、不采信转述。按风险分四批落地：写坏台账 → 静默丢数据 →
结构性双份 → 体检仪与边界。

### Fixed（真机全链路跑出来的）

- **⚠️ 台账「状态」列被清空了**（是给**所有行**清空，包括已完成的）。
  「待裁决行留空」那条改动的实现判据写的是 `... not in ("done", "default_done")`，而它比对的
  `r["结果"]` 来自 preview CSV —— 那一列写的是 `OUTCOME_LABEL[outcome]`，即**中文标签**
  （"答复完成" / "默认完成(数据修改)"），与英文码**恒不相等** ⇒ 条件恒真 ⇒ `defaults.状态` 彻底失效。
  想要的"只给待裁决行留空"被反向执行成了"给所有行留空"。
  **自检与 CI 全绿地放过了它** —— 真机 14 行**无一**写入 T 列（修后 11 行写"完成"、3 行关态留空）。
  判据改为取 `_DONE_LABELS = {OUTCOME_LABEL[k] for k in ("done", "default_done")}` ——
  **单一真相源仍是 `references/agent-contract.json`**，不另抄一份标签表。
  夹具同步补上"状态"列：原来夹具的 `column_mapping` **根本没有这一列**，那段判据**一行都不执行**，
  所以自检与 CI 照旧全绿 —— 这正是它漏掉的原因。
- **`pipeline.py run` 不把 `--config` 传给子进程** ⇒ `export_conversations.py` 只能从
  `SKILL_ROOT/config.json` 兜底取配置。实测：`--config` 指向"筛选关键字=某词"的配置，导出实际
  用了 skill 根下的**另一份**、筛到 0 个会话后中止，而报错写成"导出失败"（把用户引去查 scope）。
  若两份配置"都合法、范围不同"，则是**静默导出错误范围**。现在显式透传 `--config <绝对路径>`，
  并打印 `[pipeline] config = <路径>`（probe 与 run 都有）—— 用错配置时不再是"从输出里看不出来"。
- **`scope.groups` 里的群不过时间窗、也不过消息数** ⇒ 时间窗内 0 条的群照样产出一个只有标题行的
  **空转录文件**（实测 2 个），而空文件会进分包、被判定子代理逐个读一遍。现在跳过并显式告警
  （SKILL.md 第 2 步本就承诺"点名的群 0 条要如实报告"，至此才真的做到）。
- **自检误报：根目录一放 `config.json` 就报"README 目录树漏列 config.json"**。
  根条目的排除清单是硬编码的 `(".git", "README.md")`，而 `config.json` 正是 SKILL.md 第 3 步让用户
  复制出来的**本地文件**（`wechat_pilot/` 同理）—— **用户一复制、本地就红，CI 里没有它、永远看不见**。
  现在走 `.gitignore` 派生放行，并给 `_allowed_by_gitignore` 加了**目录模式**（`match_dir=True`）
  以匹配 `wechat_pilot/` 这种带尾斜杠的条目。同一个文件里 `smoke_doc_refs` 早就这么做了 ——
  又是"同一判据、两处实现"。

### Changed（真机验证中顺手发现的）

- 提示词里"见第 34 行那条特例"改为**按名字引用**。那句行号引用是本轮自己加的，而且**当时就指错**
  （特例实际在第 35 行）—— 硬编码行号写在散文里，加一行就漂移，且没有任何检查守得住。

### Fixed（会让台账内容出错）

- **`rules.ignore_if_no_reply` 与"数据修改类特例"作用域重叠、无优先级**：对方提改数据需求、
  我方一句话没回时两条规则都适用（忽略 vs 默认完成）—— 同一份聊天，两个子代理可能给出相反
  结果。现在**数据修改类优先**，提示词与 SKILL.md 两处写明。
- **台账「状态」列一律写 `defaults.状态`（"完成"）**：关态记下的 `rejected` / `no_reply` /
  `vague` / `pending` 是"待人工裁决"，却在台账里显示"完成"；而备注列又被强制清空 ⇒ 裁决痕迹
  彻底消失。现在**待裁决行的「状态」留空**，只有确有结论的（`done` / `default_done`）才写默认值。
  ⚠️ 这条改动的**实现判据一开始写反了**（改成了"给所有行留空"）—— 见上面 **Fixed（真机全链路跑出来的）** 的第一条。
- **`--merge` 不保证"取最早提出者"**：SKILL.md 承诺"合并一行，提出人取最早提出者"，而代码只做
  "用 b 补 a 的空字段" —— a 比 b 晚时台账记下的就不是最早提出者。现在合并前比较 `提出时间`
  （用 `parse_date`，避免 `2026/9/5` 与 `2026-09-05` 字符串比反），**自动保留较早的行**。
- **岗位剥离会丢掉括号里的岗位**：`张三（财务）` 被无条件删成 `张三`、N 列空着，而提示词说的是
  "去掉前缀 / 括号后缀"，本就隐含"括号里的岗位归到 N"。现在先判括号内容是不是岗位词，是则提取。
- **`preview` 与 `final` 的排序口径不同**：前者按 `ask_date` **字符串**、后者按 `serial()` 后的
  **数值** —— 混格式日期下两者顺序不同，人工按「序号」做 `--remove` / `--merge` 会错位。
  现在两处统一走 `parse_date`。

### Fixed（去重）

- **`ids()` 把「≥6 位纯数字」全当单号**，而通道1 是"共享单号即标重复、不看时间" ——
  电话号码、金额、纯数字箱号相同就能把两条**无关**需求并掉（并掉 = 一条真需求消失）。
  现在纯数字要求 **≥8 位**，且通道1 **也要求在时间窗内**（宁可漏标，不误并）。
- **同一会话内的重复完全无人管**：`flag_dups` 是"跨会话"去重，`chat` 相同直接 `continue` ——
  子代理拆行失误、同一个人重提都不会被标记。现在同会话也纳入，但用**更高**的相似度阈值
  （`max(similarity_threshold, 0.9)`）：同一会话前后文天然相似，沿用跨会话阈值会误标一堆。

### Fixed（提示词）

- **跨会话的答复看不见 → 需求漏记**：转录按会话分包，需求在会话 A 提出、答复在会话 B 时，
  A 的子代理按"无实质回复 → 忽略"把它丢掉。提示词现在明确告知"你看不到全部会话，
  拿不准就照常输出、把结果记为最保守的值" —— 漏记比多记难发现得多。

### Fixed（发布那一刻暴露的 —— 本地全绿、CI 全红）

> 这一组**本地没有任何办法复现**：`.gitignore` 写错不报任何错，只有代码真上了 CI 才现形。
> 而它的**第二半**更隐蔽 —— 修完第一半，"含聊天原文的产物别入库"这条兜底反而**静默失效**了。

- **`.gitignore` 的 `agent*.json` 顺手吞掉了 `references/agent-contract.json`**（子代理产出契约的
  **唯一真相源**）。同一份过宽 glob 在 `.gitignore` 与 `scripts/smoke_test.py` 的「不入库名单」里
  **各写了一份**，于是那个文件**从没被 git 跟踪**：本地有它、自检 33 项全绿；CI 与任何新克隆里
  都没有它 —— 发 v1.2.0 时 **CI 4/4 全红、17 项失败**（"找不到产出契约"）全源于此。
- **紧接着的收窄又收过头：`agent_*.json` 漏掉真机形态，兜底静默失效。** 真机产出的是
  `agent<字母>_<账号名>.json`（`_` 必须**紧跟** `agent`，而真机那里是字母），所以它**不匹配**
  `agent_*.json` ⇒ 子代理产出（含 `evidence` **聊天原文**与真人姓名）**不再被兜住**；
  而当时自检里的断言只钉了 `agent_<序号>.json` 一种（合成夹具）形态，**照样全绿**。
  这一半比第一半严重：CI 全红是**响的**，隐私兜底失效是**静默的**。
- **最终形态 `agent[!-]*.json`**：命中 `agentA_<账号>.json` / `agent_<序号>.json` / `agent<序号>.json`，
  **不**命中紧跟 `agent` 为连字符的 `agent-contract.json`。git wildmatch 与 fnmatch 两侧语义
  **各实测一遍**（`git check-ignore --no-index`），结论一致。
- **断言从"单形态"补成"两头四形态"**：真机形态两种 + 按批形态两种必须被忽略，契约与提示词模板
  必须**不**被忽略。教训写在名单上方的注释里：**单形态断言 = 单点失效** ——
  它钉住的永远只是自己恰好想到的那一种写法。

### Security

- **`.gitignore` 与仓库卫生名单补三个漏项**：子代理产出的 `agent` 开头 json（含 `evidence`
  **聊天原文**与真人姓名；它落在 `--out-dir`，一旦指到 `wechat_pilot/` 之外就没人兜）、
  `probe.json`（`probe --dump` 的产物，含会话真名 / wxid 与账号目录，README 还手把手教人 dump）、
  `history_positions.json`（人名→岗位）。
  > ⚠️ 这条的 glob **当天被改了两回**（过宽吞掉契约 → 过窄漏掉真机形态），
  > 现值见本节「Fixed（发布那一刻暴露的）」与 `.gitignore` 里的注释 —— **别在别处再抄一份。**

### Added

- **落表前的两道硬复核** —— 此前起始行算错时只打印 ⚠ 就继续，而回填是**覆盖式**的
  （kdocs 走 `update_range_data`、local 走 editor_sdk），写下去就是盖掉历史数据且无人发现：
  ① **目标行必须是空行**，且扫**整行** —— 未映射列有值也算（台账右侧的备注 / 标注列正是让
  "末数据行"算偏的原因）；② **不与上一批重叠** —— 比对 `--out-dir` 下的 `.last_write.json`
  所记行区间，挡住"拿同一份快照重跑"的重复追加。配套新增 `--force` 显式绕过。

  覆盖范围**如实说**：目标行复核发生在"自动算出的起始行"与"显式 `--start-row-excel` **且同时
  给了表格来源**"两种情况；**只给 `--start-row-excel` 而不给 `--workbook`/`--snapshot` 时无从
  复核**（一行都读不到），此时打印告警、水位那条仍生效。水位**跟着 `--out-dir` 走**（换目录即
  另一套），且记的是"已生成过 payload"而非"写入成功" —— 改完 `--remove`/`--merge` 重跑也会被
  拦下，这是**故意**的保守取舍：多拦一次的成本，远低于漏拦一次导致的台账重复追加。
- **`references/agent-contract.json`** —— 子代理产出契约的**唯一真相源**（字段 / 必填 /
  Q·R·S 值域 / outcome 标签 / 日期字段）。代码从它读，提示词仍是人工渲染版，自检逐项比对。
  此前两处各写一份、只靠代码注释里一句"一一对应"维系：单边改动会让子代理的**合法**产出被判
  "不合契约"而整批卡住，而没有任何检查会响。
- **`sheet_snapshot.py inspect --config`** —— 给了它，"追加起始行"按 config 的列映射算，与
  `final` **完全同判据**；不给则给粗算值并**显式标注**那是粗算（原先两个口径不同，照着打印的
  数字填 `--start-row-excel` 会错位）。
- **环境变量 `WECHAT_DB_ROOT`** —— 覆盖微信数据根目录的默认位置（`~/Documents/xwechat_files`
  在被 OneDrive 接管 `Documents`、或数据换了盘时不成立）。

### Changed

- **`position_columns` 不再退回写死的列位**（原 提出人=O=15 / 岗位=N=14）。缺键的语义是
  "**台账没有这两列**" —— `column_mapping` 由 `probe` 依表头名生成，而「提出人岗位」本来也不在
  `probe` 的必需列清单里。调用方分流：`build_matrix_rows`（复用只是附带功能）**静默跳过**；
  `position_reuse`（唯一职责就是写岗位列）**报错退出**。旧实现会把岗位写进完全无关的列。
- **越界「需求归类」改为归一**（新增 `normalize_q`）：已在白名单 → `QMAP` 近义 / 连写 →
  **唯一的**包含匹配；**0 候选（陌生写法）与多候选（如"优化需求"）都不猜**，原样返回交人工裁决。
  `preview` 与 `final` 共用它。
- **`export_conversations` 零导出改为非零退出**：此前 `print` + `return`（退出码 0），而
  `pipeline` 靠退出码判成败 —— "一条都没匹配到"被当成成功，整条链带着空数据跑完。
- **`--remove` / `--merge` 的序号打错改为报错**：原来只告警然后**静默忽略**（该删的没删、
  该合并的没合并，却以 exit 0 写出台账）。文档同时写明「序号」是 preview CSV 的序号列，
  **不是 Excel 行号**。
- **`find_db_storage` 多账号不再静默取第一个**：改为报错并列出候选 —— 选错账号会导出
  **别人的**聊天记录，是全流程里最不该猜的一处。
- **日期解析收敛到 `common.parse_date()`**：`serial()` 与 `flag_dups` 共用。此前两处各写一套
  （宽松正则 vs 严格 `strptime`），同一份 `2026/9/5` 一处认一处不认，跨会话去重的**时间窗
  静默失效** —— 而它失败的方式只是"少判几条重复"。
- **`people.counterparty_keyword` 与 `scope.name_filter` 互相兜底**：两者指同一个"对方关键字"
  （一个剥岗位前缀、一个筛会话），而 `config.example.json` 里两处占位符描述完全一样，只填一个
  另一个静默为空。
- **CI 矩阵只跑 Windows**（8 格 → 4 格）：主链只在 Windows 上有意义，留 ubuntu 格等于对外承诺
  跨平台。它原先唯一独有的覆盖是「大小写敏感文件系统」，已下沉到自检内部（见下）。

### Fixed

- **自检自身的"静默降级"**：`smoke_test.py` 里临时目录 `git init` 失败时只 `print` 就跳过，
  这一项仍记 `[OK]`，而它下面三条断言**一条都没跑** —— 比 `Skip` 更隐蔽（Skip 至少会在结尾
  显示「N 项跳过」）。现统一改成 `raise Skip`。
- **`smoke_doc_refs` 的文件名判断补回大小写敏感性**（`_exists_exact`：逐级 `os.listdir` 精确名
  比对）。原先用 `os.path.exists`，而 Windows / macOS 的文件系统不敏感 —— 文档里文件名写错大小写
  会被判为"存在"，只有 Linux 才红。这条此前靠 CI 的 ubuntu 格兜着，现已下沉到自检、**本地也跑得到**。
- **`position_reuse` 不再静默抬高 `--start-row`**：原 `max(start, header_row + 1)` 会把你给的行号
  悄悄改掉，岗位写到没预期的行上且不留任何提示。表格模式下"区间为空"也从只告警改为报错。
- **CI 格数守卫不再写死轴数**：原 `if len(lst) != 3: raise Skip` —— 删掉 os 轴后会走进 Skip
  （**Skip 不是 Fail**），守卫就此静默失效。现改为只要求"解析得出格数"。
- **断言只守数字与关系，不守整句措辞**：夹具条数、open 条数、CI 格数三处从"锚死整句"放宽为
  "数字与它描述的对象出现在同一行附近"。哨兵阈值（如 `SKILL.md 标题数 >= 20`）**刻意保留** ——
  它们守的是"判据本身失效"，且失败信息里写了结构精简时该如何下调。

### 验证

- 默认 **33 项** / `--full` **36 项**全绿（项数未变：新增的都是既有检查内部的断言）；
  `--full` 用带 `openpyxl` / `zstandard` 的隔离环境跑，等价 CI 的 `deps=full` 格子。
- **先红后绿注入验证**（改动走字节级备份还原，不用 `git stash`）：
  ① 台账第 7 列（**未映射列**）有值 → `final` 拒写并点出「G 有值」；`--force` 放行；
  ② 同一 out-dir 连跑两次 `final` → 第二次报「与上次区间第 3~4 行重叠」；`--force` 放行；
  ③ 文档里把真实文件名改成大写 → `smoke_doc_refs` 当场红（反引号引用与 markdown 链接两条链都验过）；
  ④ 契约里拿掉一个字段 → 红在"提示词里有、契约却没登记的字段 ['evidence']"；
  ⑤ 把「状态」判据改回英文枚举写法 → 离线端到端红在 `KeyError: (2, 6)`（状态列整列消失）。
- 新增的 `smoke_write_guard` 覆盖三条行为（目标行非空即拒 / 区间重叠即拒 / `--force` 可绕过），
  并带**端到端**用例：真跑两次 `final`，验证水位确实落了盘、也读得回来。
- **真机全链路**：解密 0.2 GB（22 成功 / 5 失败 —— 失败的都是 `.db.factory` 历史快照，无密钥属正常）
  → 10 个单聊 + 3 个群 → 3 路并行子代理产出 **14 行** → `preview` 契约校验 **0 处问题** →
  `final` 起始行自动算出 **197**、岗位复用补 **4** 个、水位记 **197~210**。
- 落表前两道复核在**真实台账**上也验过：往**未映射列**注入一个值 → 被拦并点出"该列有值"
  （`last_data_row_ws` 看不见未映射列，这正是"末数据行算偏"的成因）；同 out-dir 重跑 → 被水位拦，
  加 `--force` 放行。

### 已知缺口（诚实记录）

- 语音 / 图片 / 合并转发里的需求仍然丢失（正文被归一成 `[语音]` / `[图片]` 等占位符）——
  需要 OCR / ASR，超出本 skill 范围。
- 导出侧 800 字正文截断**无省略标记**，需求后半段静默消失。
- 子代理产出（`agent` 开头的 json，真机形态 `agent<字母>_<账号名>.json`）的落盘安全只靠忽略
  名单（现值见 `.gitignore`），五个脚本仍未挂「产物落进 git 仓库」
  告警（`common.warn_if_inside_git_repo` 定义了，但只有两处在用）。
- stdout 会打印真实联系人名 / wxid / 台账单元格内容（本机跑问题不大，贴到公开处会泄）。
- `export` 的零导出分支、日期统一的效果**没有断言覆盖** —— 前者要造真实解密库（`contact.db`
  + `message` 表），后者要构造混格式 `ask_date` 的 `flag_dups` 场景，构造成本高，暂留。
- 仍有两处行为改动**尚无直接断言**：`--merge` 取早、同会话阈值。
  （「状态列按 outcome 留空」原先也在这一列，**已补上断言** —— 夹具原本没映射"状态"列，
  那段判据**一行都不执行**，这正是它一度被漏掉的原因。）

### 无变更项

- 命令行参数、子命令、`config.example.json` 的**键**（只改注释文字与占位符描述，**没有增删键**）、
  payload 产出格式、台账列映射规则、判定规则本体 —— 全部未动。

---

## [1.1.3] - 2026-09-18

> 语义化版本判定：只动了 **CI 矩阵、自检内部的守卫与文档** —— 没有新增/改动参数、
> 没有产出格式变化、脚本行为零变化，对外能力不变 ⇒ **patch**。

### Changed

- **CI 矩阵删掉 `os` 轴，8 格 → 4 格**：`ubuntu-latest` / `windows-latest` 收敛为只跑 `windows-latest`。
  理由：主链（微信 PC 客户端 + `wcdb-key-tool`）**只在 Windows 上有意义**，留一个 ubuntu 格子
  等于对外承诺"支持 Linux"，而那条路根本跑不通 —— 这是**伪承诺**，不是覆盖率。
  逐项核过 ubuntu 格原先在守什么：非 UTF-8 控制台的编码缺陷本来就由 windows 格守（cp1252 只在
  Windows 复现）、`smoke_tmp_cleanup` 本就是 Windows 专属检查、`os.name != "nt"` 分支是 no-op
  ⇒ 几乎全是陪跑；**只有「大小写敏感文件系统」一条是真货**，且无法被别的格子替代（见下）。

### Fixed

- **`smoke_doc_claims` 的 CI 格数守卫把轴数写死了**（`if len(lst) != 3: raise Skip`）。
  删掉 os 轴后 matrix 只剩 2 个内联列表，于是走进 `raise Skip` —— 而 **Skip 不是 Fail**：
  输出仍是"✓ 全部通过（…，1 项跳过）"，这条守卫就此**静默失效**，文档与 yml 的格数再也不会被比对。
  现已改成只要求"解析得出格数"（`if not lst`），轴数变更不必再同步改守卫。
  这正是本项目一贯警惕的形状 —— "**不报错、却再也不工作**"（同族事故见 1.1.1 的 `__pycache__` 漏排除）。

### Added

- **`smoke_doc_refs` 补回大小写敏感的文件名判断（`_exists_exact`）** —— 把 ubuntu 格唯一独有的
  那条覆盖**下沉到自检内部**。原先文档引用校验用 `os.path.exists`，而 Windows / macOS 的文件系统
  **大小写不敏感**：文档里把文件名写错大小写会被判为"存在"，只有 Linux 会红。
  现在改为逐级 `os.listdir` 精确名比对，**本地也跑得到**（比原来只在 CI 那一格可跑更强）。
  配套两条扫描器自检钉住敏感性：正向必须认得 `SKILL.md`，反向必须**拒绝**一个首字母大小写不符的
  同名路径 —— 后者是这条判据与 `os.path.exists` 的分水岭，缺了它，判据哪天退化成原样也没人知道。

### 验证

- 默认模式与 `--full` 都全绿；格数断言在 4 格下**确实算出了 4**（不是被 Skip 掉的那条）。
- `_exists_exact` 做了先红后绿的注入验证：把文档里一处真实文件名临时改成大写 →
  当场红在那条 `[文件]` 上；还原后复绿（改动走字节级备份，不用 `git stash`）。
- 文档里的格数两处（`SKILL.md`「环境依赖」、`README.md`「自检」）与 yml 实际格数一致，
  由 `smoke_doc_claims` 的计数断言守着。

### 无变更项（说明为什么不是 minor / major）

- 命令行参数、子命令、配置文件字段、产出格式、判定规则 —— **全部未动**。
- 自检项数仍是默认 **32** 项、`--full` 再加 **3** 项（本次只改守卫内部，未增减检查项）。

---

## [1.1.2] - 2026-09-18

> 语义化版本判定：只动了**文档**与**自检里的守卫** —— 没有新增/改动参数、没有产出格式变化、
> 脚本行为零变化，对外能力不变 ⇒ **patch**。

### Changed

- **`README.md` 8 个顶层模块 → 5 个，191 → 141 行**。病灶不是"模块多"，而是**模块里有第二份真相**：
  「快速开始」那约 40 行命令是 `SKILL.md`「交互式流程」的副本，「目录结构」每行的脚本描述是
  「脚本清单」表的副本，而 `README` **此前不在任何守卫的比对范围内** —— 这类副本改坏了不会有提示。
  现在：命令副本压到一段最小可跑序列 + 一句指针；「实测与可靠性」并入「自检」；目录树只留名字；
  许可与合规合成一节；目录结构/自检收进「开发与仓库」。
- **`SKILL.md` 523 → 437 行**（标题数 26 → 21，守卫下限 20）：删「何时使用」（触发场景并入「30 秒速览」一行）；
  「三条不许破的规矩」由三条编号项压成一行（指针指向「核心原则」#7/#5 与「交互式流程」第 4 步）；
  「环境依赖」去掉 wcdb-key-tool 的 clone 代码块（该命令只在 README「安装」留一份）；
  「岗位复用」的 `--history` 与「独立补跑」两节合并为「降级与补跑」。

### Added

- **`references/kdocs-channel.md`**（新文件，详情层）：把 `config.excel.source = kdocs` 那一端的做法整体搬出 ——
  WPS 通道读表（两趟取数）、WPS 通道回填、云文档接口约束。`SKILL.md` 的「表格来源」节现在只留
  两通道对照表 + 四条要点 + 指针，**规则仍在 `SKILL.md`、依据仍在 `workflow-notes.md`** 的分层不变。
- **守卫 `smoke_doc_refs` 新增 C2：带文件路径的章节指针，章节名必须就在那份文件里。**
  搬一节之后，`见 \`A.md\`「那节」` 依然会被老的 C 判为"可解析"（它只查"全仓有同名标题"），
  而读者点进 A 是找不到的；C 的 `见[^\n]{0,6}「` 也管不到它（中间隔着一个反引号路径，远超 6 字符）。
  新判据一次就枚举出 3 处陈旧指针（`workflow-notes.md` 有两处把文件指向写成了 `SKILL.md`）。
- **守卫 `smoke_tmp_cleanup`：临时目录清理必须真的删掉含只读文件的目录**（判据是"删完不存在"，
  不是"调用没抛异常"）。配套把 `smoke_test.py` 里 **12 处** `shutil.rmtree(..., ignore_errors=True)`
  换成 `_rmtree_force()`（先递归清只读位再删，删不掉时**打印告警**而不是静默）。

### Fixed

- **`smoke_doc_layering` 的归一化不剥离列表前缀** —— `SKILL.md` 的规则几乎都在编号列表里
  (`1.` … `8.`)，而抄它的人多半抄成无序列表（`-`），旧判据下 `7.绝不静默…` 与 `- 绝不静默…`
  **判不出相同**，这条守卫会静默失效。实测：README 里那处逐字复述，**不剥前缀 0 命中、剥掉后 1 命中**。
  现已剥掉 `- / * / 1.` 前缀，并三条扫描器自检钉住两头（编号抄编号、编号抄无序都要命中）。
- **该守卫此前只比对 `workflow-notes.md`，`README.md` 完全在防区外** —— 门面文档最容易顺手抄一遍
  `SKILL.md`，而没有任何检查看得见。现已把 README 纳入同一比对（纳入时核过：无命中）。
- **`smoke_doc_structure` 用 `readme.split("## 目录结构")` 定位目录树** —— 子串匹配意味着标题一旦
  降级成 `### 目录结构` 仍会命中（`### …` 里含着 `## …`），**能跑纯属巧合**。现已改成锚定整行的
  `^#{2,4}[ \t]*目录结构[ \t]*$`，并断言"节后第一个围栏块就是目录树"。
- **`smoke_doc_layering` 的 `>` 引用块豁免对 `README.md` 同样生效** → 把 `SKILL.md` 的整句抄进
  README 的引用块就能免检。现已**只对 `workflow-notes.md` 保留豁免**（那里的 `>` 是"引述旧结论并标明
  作废"的专用语法，豁免有依据），README 只豁免标题与表格行。
  > **这个"修复"本身一度是假的，被注入测试当场抓出**：只去掉豁免是不够的 —— `_norm` 当时**并不剥
  > `>` 前缀**，抄的人只要保留 `> ` 就照样免检。随后把 `>` 并进前缀剥离（与 `-` / `1.` 同一条正则）。
  > 现在三条自检钉住这处差异：`>` 行在 README **要参检**、在 notes **仍豁免**、抄成引用块**必须判得出重复**。
- **`smoke_doc_cli_flags` 的扫描范围从 `SKILL.md` 扩到 `README.md`**（以及新搬出的 `kdocs-channel.md`）：
  README 的「快速开始」是给人照抄的最小命令序列，此前**命令写错形状没有任何检查会响**，而读者照抄会直接报错。
  判据仍只守**形状**（`pipeline.py <子命令> … --config` 这种错序），**不要求** README 的命令与
  `SKILL.md` 逐条同名 —— 那段"能立刻跑起来"的序列是 README 的职责。讲解"这样写会被 argparse 拒"的
  反例行**豁免**（它们本来就该那么写），并断言豁免分支**确实被走到**（反例不见了就该同步删豁免）。
- `SKILL.md` 标题数下限（20）的失败信息改成**给下一步动作**：若确实是结构精简所致，就同步下调阈值并写明
  理由，**不要**为了过线把标题硬撑回去 —— 这类哨兵断言最怕被当成"随便塞两个字就能过"的门槛。
- `DOC_FILES` / `DOC_NORMATIVE` 纳管新增的 `references/kdocs-channel.md`
  （新增 `.md` 不进守卫会被「磁盘 vs 文档」那条当场拦下）。

### 验证

- 默认模式与 `--full` 都全绿（本机缺 `openpyxl` / `zstandard` 时那 2 项按设计 Skip，CI 的 `deps=full` 格子覆盖）。
- 四处新/改动的守卫都做了**先红后绿**的正向验证（不用 `git stash`，靠字节级备份还原并比对内容）：
  ① 往 README 注入一行与 `SKILL.md` 编号列表逐字相同的规则 → 当场红在那条 bullet 上；
  ② 把 `workflow-notes.md` 的指针文件指向改回 `SKILL.md` → 当场红在那处指针上；
  ③ 往 README 的 `>` 引用块注入 `SKILL.md` 原句 → 红（**第一次没红，正是它抓出了上面那个假修复**）；
  ④ 把 README「快速开始」的 `--config` 挪到子命令之后 → 红。四处还原后均复绿。
- **只读删除的失败模式先复现再动手**：临时目录里 `git commit` 后 `.git/objects` 有 3 个只读文件，
  `rmtree(ignore_errors=True)` **删不掉任何一个**。但**当前自检跑不出残留**（唯一的 git 操作是空仓库
  `git init`，不产出对象）—— 默认与 `--full` 各跑一遍，`%TEMP%\wm_*` 残留都是 **0**。
  所以这次是**廉价保险**而不是"正在流血"：`%TEMP%` 里曾积过的 119 个目录来自历史版本或中途被杀，
  当前代码复现不出来。同族的 `skill-release` 真栽过（积了 43 个 `sr_orch_*`）。
- 顺手踩到并记录一个环境坑：验证脚本里用 Python 默认参数改写 README 会因 Windows 的
  **换行翻译写出 CRLF**，被「仓库卫生」那条（`.gitattributes` 的 `eol=lf`）当场抓住 —— 改这些文件必须
  按字节写或显式 `newline=""`。

### 无变更项（说明为什么不是 minor / major）

- 命令行参数、子命令、配置文件字段、产出格式、判定规则 —— **全部未动**。
- 自检项数默认 31 → **32**（新增的两条都在自检内部，对外能力不变），`--full` 仍是"再加 3 项"。

---

## [1.1.1] - 2026-09-18

> 语义化版本判定：只修了**自检脚本**里的一个缺陷 —— 没有新增参数、没有产出格式变化、
> 对外能力不变 ⇒ **patch**。

### Fixed

- **`smoke_test.py`「README 目录树 vs 磁盘」检查漏排除 `__pycache__`** ——
  该检查读 `os.listdir(scripts/)` 时只过滤了 `.` 开头的条目，而 `__pycache__`
  **不以 `.` 开头**，于是被当成"README 漏列的文件"。在**会生成 `.pyc` 的环境**
  （如 GitHub Actions，它不设 `PYTHONDONTWRITEBYTECODE`）里，8 个 matrix 格子
  **全部误报失败**。
  同函数另外三处磁盘扫描都已排除 `__pycache__`（行 1982 / 2005），**唯独这一处漏了**。
  现已对齐，并在注释里写明这个环境差异。

  > **本地为什么看不出来**：本机托管 Python 设了 `PYTHONDONTWRITEBYTECODE=1`，
  > 根本不生成 `__pycache__` —— 典型的「本地全绿、CI 全红」。
  > 凡"扫描磁盘文件"的检查，都必须显式排除运行时产物，否则本地环境会掩盖问题。

- **`common.warn_if_inside_git_repo` 在 Windows CI 上静默失效** ——
  它用 `os.path.commonpath([root, d])` 判断"产物是否落在 git 仓库内"，但 Windows 上
  `TEMP` 常是 **8.3 短名**（形如 `...~1\AppData\Local\Temp`），
  而 `git rev-parse --show-toplevel` 返回的是**长名** —— 两者**字符串毫无共同前缀**，
  `commonpath` 算出来是一个很浅的祖先目录而不是仓库根 ⇒ "在仓库内"被误判成"不在"，
  **告警静默失效**（G1 那道兜底机制等于白设）。
  现已用 `os.path.realpath`（Windows 上即 `GetLongPathName`）把两边都展开成长名，
  再用 `os.path.normcase` 抹掉盘符大小写差异。

  > **本地为什么看不出来**：本机 `TEMP` 恰好是长名，**永远复现不了** ——
  > 又一个"只有 CI 的 windows 格子会红"的环境差异。
  > 这个缺陷从 v1.0 起就在，只是此前 CI 从未走到过这一项
  > （`__pycache__` 那条更早失败，把它挡在后面）。

  > 注：`v1.1.0` 这个 tag 上的 CI 是红的（就是上面这个缺陷），本版修复。
  > tag 与 Release 均保留不动，不去改写已发布的历史。

### 无变更项（说明为什么不是 minor / major）

- 命令行参数、子命令、配置文件字段、产出格式、判定规则 —— **全部未动**。

---

## [1.1.0] - 2026-09-18

> 语义化版本判定：本批含两类改动 —— ① CI 接入、自检扩充、修一批缺陷（这些单独看是 **patch** 级）；
> ② **新增 `scripts/rules_check.py`（`plan` / `verify` 两个子命令）+ `references/rules-fixtures.json`**
> —— 这是**新子命令、用户能调到的新东西**，按"对外能力有没有变"的判据属 **minor** 级。
> 整批取 `1.1.0`。

### ⚠ 升级注意（**哪些调用现在会失败 / 行为变了**）

本批的主力是"把**静默错误**变成**显式报错**"。这类改动对已把它接进自动化的人是**可见的行为变化**，
所以在此逐条列出；也正是这一节，让它算 minor 而不是 major ——
被剥夺的只有"错误的用法"（静默无操作 / 静默崩 / 路径打错仍产出），没有任何**正常**用法被禁止。
**没有任何旧参数被删除，也没有任何产出格式被改。**

| 调用 | 旧行为 | 新行为 | 你需要做什么 |
|---|---|---|---|
| `position_reuse.py` 不给 `--new-rows`/`--start-row` | **exit 0**、静默 0 条 | 报错退出 | 任选一个：给 `--new-rows <final_rows.csv>`（推荐）或 `--start-row <行号>` |
| `final --preview <旧版产物>`（列名带列字母，如「提出人O / 岗位N」） | `KeyError` **裸崩** | 明确报错并列出缺失列 | 重新跑 `preview` 生成新产物 |
| `final` 的 `--workbook`/`--snapshot` **路径打错** | **exit 0**，起点退回 `config.excel.start_row`、照常产出 payload | 报错退出 | 修对路径（这是本批修的 P0） |
| `preview` 的子代理产出不合契约（缺 `O`/`L`/`ask_date`、`Q`/`outcome`/`R`/`S` 越界、日期解析不出） | 静默通过，错值写进台账 | 报错退出 + 完整清单落 `validation_report.txt` | 修子代理产出或提示词；**没有跳过开关** |
| `final --snapshot` 且快照行数够不到历史区 | 告警后跳过岗位复用 | **报错**（不复用时加 `--no-reuse-position` 即不检查） | 把「第 2 趟读表」的行读全；或改用 `--start-row-excel` 且不带快照 |
| `split_for_agents.py` 找不到转录文件 | 打印 `✗` 但 **exit 0** | exit 1 | 若上游靠退出码判断，现在能正确感知失败 |
| `--worksheet-id` 从 CLI 传入 | **字符串**（与 config 的整数不一致） | 统一为整数（`'7'` 仍照收） | 无需改动 |

两处**产出值**的变化（不是格式变化）：`优化或需求` 这个连写值现在归一到 `需求`；Q 白名单外的值会被
**显式告警并列出序号**（仍原样写入，不擅自改）。

### Added

- **`scripts/rules_check.py`** + **`references/rules-fixtures.json`** —— 把"改判定规则必须做行为验证"
  从**靠人临时做**变成**可复跑**。
  - **为什么需要**：静态断言只能守住枚举/开关三处一致这类**形状**，守不住规则本身对不对；
    而此前那套流程（造夹具 → 派独立子代理盲评 → 比对）每次都得现搭，于是规则的正确性
    **没有任何可复跑的东西**。
  - `plan`：渲染提示词（占位符 + 依 `config.rules` 生成的**开关覆盖表**）→ 写 `prompt_rendered.txt`；
    并为每条夹具生成一个**不含期望答案**的盲评 case 文件（盲评是硬要求，带答案等于让子代理照抄）。
  - `verify`：读回子代理的 `case_<id>.json` → 与标注比对 → 差异分三类并 exit 非 0：
    **误记**（该忽略却记了）/ **漏记**（该记却没输出，比误记更危险）/ **结果不符**；
    另有"待裁决"（规则本身没定义的）与"未判定"（缺结果文件，不算通过）。
  - **渲染成为唯一实现**：关态句的权威来源就是提示词里的【开关渲染表】，脚本只读它、不另存一份；
    渲染时先剥掉开头给执行代理看的操作说明（否则那句 `{{...}}` 会永远残留、"不得残留占位符"的
    校验等于没守），再注入占位符；**渲染后仍有未替换的占位符即报错**（新加占位符忘了登记会立刻暴露）。
  - 夹具 **18 条**，按三层必备组织：每个判定开关两态各 ≥1 条（且该条 on/off 期望**必须不同**，
    否则等于没测这个开关）、每条规则的**排除/保留双向成对**（只测一个方向会**误杀**——
    修「系统特别卡」误记时必须同时确认「搜一个箱号要等 15 秒」仍按 bug 记）、`outcome` 枚举全覆盖。
    每条带 SKILL.md 规则锚点与一句为什么；样本一律自造脱敏（用 `[我方]`/`[对方]` token 渲染）。
  - 顺带**暴露一条规则本身的缺口**：`reply-with-ops-action`（对方给的是可定位对象、我方只回运维动作）
    该落哪个 outcome，规则**从未定义** ⇒ 该条标 `open: true` 并登记进 `_open_questions`，
    `verify` 对它只报告不计失败。**不假装有答案**。
- `smoke_test.py` 新增第 6 组断言 `smoke_rules_fixtures`：**CI 侧只校夹具完备性**（7 项：id 唯一 /
  转录非空 / 开关名真实存在 / 每开关有「两态期望不同」的样本 / **锚点在 SKILL.md 里真实存在**
  （防悬空引用）/ 标注自洽（不记录 ⇒ outcome 必须属被忽略类）/ outcome 枚举全覆盖 /
  脱敏扫描 / `open` 标记与 `_open_questions` 一致）。
  **真实判定不进 CI** —— 它要模型且非确定性，塞进去只会 flaky。
- **`.github/workflows/selftest.yml`** —— 自检接进 GitHub Actions，8 格矩阵：
  `os × python × 依赖`。
  - **OS 跨 ubuntu / windows**：Windows 的标准流编码是 cp1252，中文输出会崩，**只在 Windows 复现**。
  - **Python 跨 3.9 / 3.13**：3.9 是 `SKILL.md` 声明的最低支持版本。
  - **依赖轴 minimal / full**：`minimal` 一个第三方库都不装，守「零依赖也能跑」与
    「缺库给可执行提示而非裸 traceback」；`full` 装上 openpyxl / zstandard 才跑得到
    **本地通道读表**与 **ZSTD 成功解压**这两条真实路径。
  - CI 里**故意不设** `PYTHONUTF8` / `PYTHONIOENCODING` —— 设了就把 Windows 那个编码缺陷盖住，
    windows 格子等于白跑。另加一格「**自检不得污染工作区**」：跑完 `git status --porcelain` 必须为空。
- **`.gitattributes`**（`* text=auto eol=lf`）—— 让不同平台的检出**逐字节一致**。
  注意一个**被实测证伪**的说法：CRLF **不会**让上面那格「工作区干净」检查假红。
  在临时目录克隆两份做 `core.autocrlf=true` 对照：不加本文件时 windows 检出有 20 个文件带
  CRLF，但 `git status --porcelain` **仍为空**（git 入库归一 LF、检出转 CRLF、比对时再归一，是自洽的）。
  所以本文件的理由是"消除跨平台换行噪声、让换行差异显式可见"，与那格检查无关。
- **`common.ensure_utf8_stdio()`** —— 把 stdout / stderr 显式切到 UTF-8（`errors="replace"`），
  并在 Windows 上把控制台代码页也切过去。`__main__` 入口共 **11 处**（`pipeline.py` + `scripts/` ×10）
  在**任何输出之前**调用它。
- `smoke_test.py` 新增 **6 组**断言（13 → 17 项，`--full` 下 14 → 20）：
  - **CLI 守卫契约** —— 这些出口全部表现为 `SystemExit` + 一句中文提示，**读代码测不到**，
    只有跑真进程才守得住：`position_reuse` 不说清「哪些行算本批新行」、`final` 拿不到追加起始行、
    旧版 `.xls` 给可执行提示而非裸 traceback；外加 6 个入口的 `--help` 冒烟。
    夹具自带 config + 快照 —— 否则会先死在 `column_mapping 未配置`，**根本到不了要守的那条守卫**。
  - **离线端到端 golden 链路** —— `preview CSV → final（快照通道）→ payload.json → to_kdocs_payload
    → rangeData`，断言列映射、追加起始行、岗位复用补的 2 个「客服」、提出时间落成 Excel 序列号、
    单格 formula op、日期 format op、分批上限。全程不碰 openpyxl、网络与微信，两种依赖格子里都跑。
  - **仓库卫生** —— `git ls-files` 不得含运行产物、跟踪文件不得出现绝对个人路径、必须是合法 UTF-8。
    `gitignore` 写错**不报任何错**，而这类产物含聊天明文与账号目录，只能靠断言守。
    扫描器自带自检（规则放宽 → 漏报、规则被写坏 → 误报，两头都静默）。
  - **cp1252 回归守卫** —— 子进程把 `PYTHONIOENCODING` 设成 `cp1252` 后跑真命令，
    本地就能复现 Windows runner 的条件，不必等 CI 反馈。
  - `--full`：**本地表格通道**（openpyxl 造临时 xlsx → `probe` 读表头与追加起始行）；
    **ZSTD 成功解码**（含压缩的「引用回复」——正文在 `<refermsg>` 里，只取 `title` 会整条丢需求）。
- `smoke_test` 的 `Skip` 机制：环境不适用时显式打 `[SKIP]`，**不计入通过**。
  与「绝不静默」的原则对齐 —— 否则会出现"绿着但根本没跑"。
- **`preview` 新增子代理产出契约校验**（`validate_agents` / `render_agent_report`）：
  硬拦**形状**（顶层必须是数组、元素必须是对象）、**必填**（`O`/`L`/`ask_date`）、
  **值域**（`outcome` / `Q` / `R` / `S`）、**日期可解析**（三个日期能被 `serial()` 解析 ——
  解析不出就会写出空格子，还会把这行沉到排序末尾）；**契约外的字段名只告警**
  （它通常是"提示词改了、实现没跟"的前兆）。
  - 为什么必须**硬拦**：子代理的错值不会当场报错，而是顺着 `preview → final` **一路写进台账**。
    旧实现只做宽松兜底（`r.get(...)`），字段拼错 / 枚举越界 / 日期写成"5月14日"全部静默通过。
  - 报告按「类别 × 文件」汇总，屏幕每类列前 10 个具体位置，**完整清单落
    `<--out 同目录>/validation_report.txt`**（截断只影响屏幕，不影响文件 —— 上百行时一个共性
    字段拼错会刷满屏把关键信息淹掉）；**不提供 `--skip-validation`**：与本项目既有立场一致
    （缺关键输入、拿不到起始行都是直接报错退出），个别行不想要请走裁决环节删行/合并。
  - 文件选择刻意**不是**"所有 `*.json`"：`_out` 下还有 `payload.json`（dict）这类产物，
    按契约拦会把它们全拦死。规则是 `agent*.json` 一律强校验（叫这个名却写成 dict = 该产出没产出），
    其余 json 只有**本身是数组**时才纳入（因为 `load_agents` 会把数组型 json 一并消费）。
- **快照记录读表覆盖范围 + `final` 两条前置检查**（`common.snapshot_sheet_coverage`）：
  `build` 给每张子表写 `coverage`（**裁边之前**的原始行列极值）与 `trimmed`（裁边后的实际长宽）；
  `final` 在启用岗位复用时据此判两条 —— **行够不到历史区 → 报错**（例：快照 3 行 vs 起始行 197）、
  **列没覆盖到 提出人/提出人岗位 → 告警**（两可：确实没读，或该列历史区本来全空）。
  用"裁边前"的极值而不是 `rows` 的实际长宽，是因为尾部空行 / 右侧空列会被裁掉，
  拿裁边后的值判断会把这两种情况误报成"没读到"。
  旧快照没有 `coverage` 字段 → 两条检查都跳过，**不误报**。
- **`SKILL.md` frontmatter 长度额度断言**（`smoke_skill_frontmatter`）：
  `description` 是路由真正读的字段，每次补触发词 / 改描述都在吃平台侧 **1024** 的额度，
  而**超限不会报任何错**（不拒绝加载、也不截断告警）—— 只能靠断言守。
  兄弟 skill（`webbuilder-xwl-patch`）就是**靠人工撞线**才发现这条的（加触发词时撑到 1029）。
  - 度量口径沿用兄弟 skill（**字符**），但同时把**字节**数一并报出来：
    ⚠️ 平台按字符还是字节计，**没有权威依据**；中文 1 字 ≈ 3 字节，两者差 2 倍以上。
    任一口径用掉八成额度就打提示 —— 本仓**当前正落在这一档**：`frontmatter` 836/1024 **字节**（余 188）。
  - `description` 的写法（块标量 `>-` / `>` / `|-` / `|` 与同行标量）**两种都认**；
    认不出就**报错**而不是静默按 0 处理 —— 否则换个写法就等于把守卫绕过去。
- **文档分层断言**（`smoke_doc_layering`）：见 `### Changed` 的「文档分层重构」——
  `workflow-notes.md` 不得逐字复述 `SKILL.md` 的长行。
- `SKILL.md` 开头新增 **「30 秒速览」**（+1259 字符 / +2459 字节，**纯插入**、插入点之后正文**逐字节未动**，
  frontmatter 未动，总体积 **+7.4%**）。
  起因是 **darwin-skill 八维基线评估**：8 个维度里只有「整体架构」拿了 8 分 —— 445 行的主文档
  **开头缺一个导航层**，速查表要到第 391 行才出现，第一次读的人得从头翻。
  - 内容四块：**做什么**（含"历史行永远只读"）、**先认通道**（`local` / `kdocs` 的判据表，
    并点明"其余环节两条通道算法完全一致"）、**主链五步**（① probe 两趟 ② 一次性确认
    ③ 写 config ④ run→preview→final ⑤ 回填，每步一句重点）、**三条不许破的规矩**。
  - 另加一张 **「卡住了先看哪儿」** 症状→章节索引表（读不到表 / 岗位列空 / 该不该记这一条 /
    写回失败 / 参数记不住），以及一句"首次使用前先看环境依赖（第三方解密工具须用户同意）"的提醒。
  - 新增段落引用的 8 个章节名**逐个核实存在**（防悬空引用）。
  - **效果维重测**（darwin 要求效果必须由独立子代理评，不能自己改自己评）：改后带 skill 臂
    在同一个测试 prompt 上的计划**没有变差**，反而更成体系（直接按"五步流水线"组织）。
    ⇒ 结构维 8 → 9，总分 **88.5 → 90.0**，按 darwin 的棘轮规则 **keep**。
- **`rules_check.py` 的正向路径补进 CI**（此前**零**回归覆盖 —— smoke 里它只出现过两次，
  且**都是错误路径**：坏 JSON config / `--results` 缺失。而 `verify` 的三类差异分类正是本支的核心价值，
  此前只手工跑过一次。分支② 的产出契约校验反而有完整断言，两边不对称）。
  只守**确定性**的那一半，真实判定仍由独立子代理跑、不进 CI：
  - `plan`：渲染后**不得残留 `{{`**、占位符必须注入真实值、开关覆盖表已注入、
    case 文件数 == 夹具条数、说话人 token 已渲染；
    **每个 case 逐个反查不含"答案痕迹"**（`期望` / `应记录` / `应忽略` / `误记` / `漏记` / `标注`
    六个词 + 夹具 `why` 原文）—— 盲评是这套机制的地基，case 文件一带答案，验证就整体失效。
  - `verify`：造「全按标注」与「三类差异各一条 + 一条未判定」两份结果，
    断言前者 exit 0、后者 exit 非 0 且**四个桶都恰好各 1 条、并点名到具体是哪条夹具**
    （选四条**互不相同**的夹具当角色 —— 我第一版把"应记录"与"结果不符"选到了同一条，
    后面的 `elif` 把前面盖掉，"结果不符"永远不触发，是这条断言自己抓出来的）。
- **仓库卫生断言改成双向**（`.gitignore` ↔ 名单必须同步），并给「产物落进 git 仓库」加告警 —— 见 `### Fixed`。

- **文档结构类守卫固化成常驻断言**（`smoke_doc_refs` / `smoke_doc_structure` / `smoke_doc_claims`；
  自检 28 → **31** 项，`--full` **34**）。此前这类检查只是**一次性探针**，跑完就再没人跑 ——
  而**真踩了**：结构重排改了 `workflow-notes.md` 一节的名字、却没同步那句「见「…」」，
  悬空引用就这样进了仓库，而**现有守卫一条都看不见**（它既不是坏 JSON，也不是不一致的枚举）。
  引用与结构类缺陷的共同点是：**不报错，只有读者会点空 / 照做会跑不通**。
  - `smoke_doc_refs` —— 四类指向必须可解析：**文件引用**（反引号里的路径）、**markdown 链接**、
    **章节指针**（`见|详见|参见 + 「…」`）、**参数速查表逐行归属**（该行的参数必须属于该行那个脚本）。
  - `smoke_doc_structure` —— 结构自洽：标题层级不跳级、**同一父节下**不重名（比全文查重精确：
    CHANGELOG 每个版本都有 `### Added`，父节不同就不算重名，于是**不必给 CHANGELOG 开例外** ——
    一开例外就同时挡住它里面真正的重名）、顶层有序列表编号从 1 连续、README 目录树与
    `SKILL.md` 脚本清单表**逐项对齐磁盘**、新增的 `.md` 必须纳入守卫。
  - `smoke_doc_claims` —— 文档**声称的事实**必须与仓库现状一致：术语等价声明在位、
    **已作废的说法不许当现行**（按行判 + 留痕豁免）、夹具条数 / open 条数 / CI 格数 /
    README「当前版本」与 CHANGELOG 最新版本段**多处一致**。
  - 三条都带**扫描器自检**（漏报与误报两头都钉：既断言"假目标会被抓到"，也断言
    "真目标不会被误伤"，还断言"扫描覆盖面没塌"），并配 **18 条负向测试**
    （逐条弄坏一次，确认报红的是**预期那条**检查）。
- **运行期产物的"放行名单"改为从 `.gitignore` 派生**，不再另抄一份：本项目已在"同一事实抄两份"
  上栽过多次（README 里那份 `.gitignore` 副本曾漏列 15 项）。现在 `.gitignore` 中**不带斜杠**的
  条目用 `fnmatch` 直接参与判定，并断言"派生真的在工作"——既不能退化成"什么都放行"，
  也不能"什么都没放行"。

### Fixed

- **两处"会在发布那一刻变味"的写法**（用户说"我马上要发布"之后，专门扫了一遍全仓写死版本号的位置）：
  `CHANGELOG` 头部原写「已打 tag 的是 `v1.0.0` 与 `v1.0.1`」—— 发布 1.1.0 的**同一分钟**它就成了错的，
  而**没有任何机器检查会提醒你**（tag 存在性刻意不校验：在"版本已定、tag 未打"的常态下，
  那种断言会长期假红，而**长期假红的守卫比没有守卫更糟**）。
  已改成"以文末版本链接区为准、此处不列举"，并把「`tag` 按发布时机补上」也去掉了时态依赖。
  另加一条守卫把这个形状钉住：**正文里不许出现「已打 tag 的…vX.Y.Z」这类枚举** ——
  它守的不是"列表对不对"，而是"**不许依赖 tag 事实**"。
- **自检项数只守了一头**：`smoke_test` 早有「README 声明的 N 项 == 实际项数」，但那只管**默认模式**；
  `README` 里「`--full` 再加 N 项」是**另一头** —— 加一条 `--full` 专属检查却忘了改文档时，
  不会有任何报错。已补成双向：**默认模式核总数、`--full` 模式核增量**，各在一个模式里跑，合起来才闭环。
- **`SKILL.md` 的「主链五步」与解密时机自相矛盾**（独立评估子代理指出，已逐条实测核实）：
  速览把解密放在 ④ `run`，而 ②「一次性确认」要把**会话清单**呈现给用户选 —— 会话清单却
  **只存在于解密库里**。照速览的顺序走会卡在"没有会话可确认"。已把解密显式放到 ① 与 ② 之间，
  并点明 ① 为什么要两趟、④ 什么时候该加 `--skip-decrypt`。
- **`excel.kdocs.link` 是"看起来能用、其实没有代码读"的键**：文档写成"定位三选一：
  link（推荐）> file_id > name"，读起来像代码实现的优先级阶梯；实测**没有任何脚本读 `link`**——
  把它换成 `file_id`/`drive_id` 是 **agent** 的活（调 `get_share_info`）。
  已在校验表与 `config.example.json` 注释里写明"脚本真正读的只有 `file_id`/`drive_id`/
  `worksheet_id`/`date_numfmt`"。
- **`test-prompts.json` 的期望描述停在旧流程**：仍写着"→ 人工裁决 → `position_reuse` → 回填"，
  而岗位复用早已**内置进 `final`**、`position_reuse` 降级为事后补跑。这份文件是技能质量评估的输入，
  停在旧流程会**把评估带偏**。已改成"→ `preview` 裁决 → `final`（内置岗位复用）→ 回填"。
- **章节指针守卫的判据曾经形同虚设**（被自己的负向测试抓出来）：原判据是"标题含引用名 **或**
  引用名含标题"，后一个方向让仓库里**任一短标题**（哪怕是个 `# x`）都能给"任意含该字母的长句子"
  背书 —— 自检里"假章节名竟然解析成功"那条断言就是这么红的。已收紧为只认"标题包含引用名"，
  并留了两向自检钉住这个方向。
- **"已作废说法"的豁免判据过松**（对抗性尝试抓出来）：原判据是"同一行里出现豁免词即可"，
  行尾随便写个"不存在"就能把作废结论重新放回来。已改成豁免词必须落在旧说法两侧 **±60 字符**内
  （本仓现有 5 处留痕全在窗口内，实测过），并加了"豁免词离得老远不该生效"的自检断言。
- **新功能的产物没被 `.gitignore` 纳管，而默认落点就在仓库里**（由"全文件对抗性检测"查出）：
  `rules_check.py plan` 不给 `--out` 时默认写 `<cwd>/rules_check_out` —— 从仓库根目录跑一次，
  `git status` 就冒出 `?? rules_check_out/`；里面 19 个文件，其中 **`prompt_rendered.txt`（7.2 KB）
  含渲染后的全部真实标识**（处理人姓名、**我方标识＝微信号**、对方关键字、服务对象、业务系统描述），
  18 个 `rules_cases/*.txt` 也各带渲染后的对方关键字；
  `validation_report.txt`（分支② 新加，写在 `--out` 同目录）同样没被忽略，而它含
  **agent 文件名**（真机形态是 `agent<字母>_<账号名>.json`，账号名往往是**人名拼音**）与违约值。
  ⚠ 本条早期版本在括号里**举了真机文件名的实例**，等于把真人姓名拼音写进了仓库 ——
  已改为占位符，并补一条守卫（见下）。
  这个仓库**历史上出过一次"真实客户消息原文当测试夹具"的泄漏**，属同一类。
- **卫生断言的判据点太晚（结构性失明）**：它只看 `git ls-files`（**已跟踪**文件），
  所以只能发现"产物**已经**被提交"，发现不了"产物**即将**被提交"（未跟踪且未忽略）——
  上面那条正好落在这个盲区里；它内部的名单也没跟新功能同步。
  修法三件（详见 `### Added`）：`.gitignore` 补齐、**断言改双向**、
  `plan`/`preview` 在**产物落进 git 仓库**时打醒目告警
  （`common.warn_if_inside_git_repo`；判据是 `git rev-parse --show-toplevel` 且产物在其下 ——
  实测本机工作区**不是** git 仓库，所以日常用法不会误响；告警本身有正反两向断言守着）。

- **Windows 上输出中文会直接崩**（真缺陷，**只在 Windows 上复现**）：
  实测 GitHub 的 `windows-latest` runner 标准流编码是 **cp1252**，而本 skill 的输出**全是中文**
  ⇒ `print` **第一行**就 `UnicodeEncodeError` 并 exit 1。本地（git-bash / 本机工作区）都是 UTF-8，
  所以本地怎么测都是绿的。⇒ 由 `ensure_utf8_stdio()` 修，并用子进程断言钉住。
- **`to_kdocs_payload.py` 的 `worksheet_id` 类型两条路径不一致**（由新增 e2e 断言查出）：
  从 CLI 传（`--worksheet-id 7`）是**字符串**，从 `config.excel.kdocs` 读是**整数**，
  同一个键产出两种请求体。现统一成整数：`--worksheet-id` 加 `type=int`
  （`sheet_snapshot.py build/plan` 同步），并保留对 config 侧字符串的归一兜底。

**（第二轮：全量审查查出的缺陷，同一版本内一并修）**

- 🔴 **`final` 把「路径打错」当成「没给表格来源」，静默降级并 exit 0** —— 本轮最严重的一条。
  它用 `os.path.isfile()` 过滤 `--workbook / --snapshot`，路径不存在时两个分支都不进 →
  `sheet=None` → 追加起始行改从 `config.excel.start_row` 取，**并且以退出码 0 产出 payload**，
  可直接回填到错误行。这与同文件"拿不到起始行就报错退出"的既定原则直接冲突
  （那句只防住了"完全没给参数"）。现在：**显式给出却不存在 → 直接报错退出**，绝不退回 config 值；
  仅当同时给了 `--start-row-excel` 时才允许 config 里的 workbook 缺失（并打印说明）。
  `pipeline.py probe` 的同名参数一并改为明确报错。
- **8 处「路径不存在」抛裸 traceback**（probe / final --preview / position_reuse 的
  `--snapshot·--workbook·--history·--new-rows` / sheet_snapshot inspect / preview --src）——
  而项目对同类输入（`--snapshot`、`--raw`、`--payload`）一向给可执行提示，
  **同一类输入两种行为**本身就是缺陷。现全部收敛到 `common.require_file / require_dir /
  load_json_file / open_local_workbook`（唯一入口，与 `require_openpyxl` 同一思路）。
  顺带修正顺序：**先判存在与扩展名，再要 openpyxl** —— 否则"既打错路径又没装库"时，
  用户看到的是"请装 openpyxl"，而真正的问题是路径写错了。
- **`split_for_agents.py` 用退出码 0 表示失败**：打印 `✗ …` 之后 `return`，
  而 `pipeline.py run` 靠退出码判断成败 ⇒ "没有转录文件"会被上层当成跑通。改为 `sys.exit`。
  另修：`os.makedirs(out)` 会把**打错的** `--transcripts` 路径连带创建出来，
  于是用户得到一棵凭空冒出的空目录树 —— 现在先校验目录存在。
- **「需求归类」（Q 列）取值域三方不一致，`QMAP` 是一条永不命中的死映射**：
  提示词输出枚举是 `数据处理|优化|需求|bug|答疑`，规则文本却写「新增或优化→**需求或优化**」，
  而 `QMAP = {"优化或需求": "需求"}` 的键**任何一方都不会产出**（除非模型自由发挥）。
  实测线上台账「需求归类」列 **196 格**：数据处理 104 / bug 31 / 答疑 27 / 优化 24 / 需求 9 ——
  **`优化` 与 `需求` 是并存的两种值**，连写值 `优化或需求` 在台账里从不存在。
  ⇒ 规则文本改成明确二选一（新增→`需求`，改进→`优化`），新增 `Q_VALUES` 白名单，
  `final` 遇到白名单外的值**显式告警并列出序号**（不静默写入、也不擅自改值），
  并新增断言守「提示词枚举 ⊆ 白名单 ∪ 归一映射 且反向可表达」。
- **`--new-rows` 的 CSV 缺「提出人」列时静默产出 0 条并 exit 0**（典型来源：台账模板没有该列，
  于是 `final_rows.csv` 也没有）⇒ 现在缺列直接报错；全部人名为空时额外告警。
- **preview CSV 列名不匹配时报 `KeyError`（裸 traceback）**：真机上 2026-09-11 那轮产物的列名是
  带列字母的（`提出人O / 岗位N / 提出日期M / 需求描述L`），拿它喂当前的 `final` 会崩在第 N 行。
  现在先校验必需列，缺失就报错并说明"旧版产物的列名不一样"。
- **`final` 的 merge 标记是死逻辑**：合并时往 `备注` 追加 `[并入原#b]`，而 `final` 随后无条件
  把备注清空、且 CSV 只按 `mapping.keys()` 取值 ⇒ 该标记**任何产物里都不会出现**。
  按"备注只写业务结果"的约定删掉该追加（合并信息由 stdout 的 `[merge]` 行给出）。
- **`position_reuse` 的「无法由表格推算起始行」是死分支**：`next_append_row_ws()` 恒返回 ≥2，
  `if not start` 永不成立，给人"这里防住了"的错觉。已删。
- **`probe` 的处理人候选命中第一个列名就 `break`**：若「解决人」列存在却整列为空，
  就不会再试有值的「责任人」列 ⇒ 候选为空。改为**取到候选才停**。
- 删掉两个**无调用点**的函数 `common.next_append_row()` / `next_append_row_snapshot()`；
  `references/workflow-notes.md` 里那句推荐用法的引用同步改为指向真实在用的
  `common.next_append_row_ws`（否则文档在推荐一个没人调用的入口）。

> **错误路径实测**：17 个「不存在 / 坏 JSON / 缺参数」场景，修前 **9 个裸 traceback + 1 个 exit 0**，
> 修后 **17/17 全部友好报错、退出码非 0、无 traceback**（可用 `probe_error_paths.py` 复跑）。

**（第四轮：实施 `rules_check.py` 时顺带把"坏 JSON"这一档补上，又扫出 3 处）**

- **配置文件本身写坏 → 每个入口都抛裸 `JSONDecodeError`**：`common.load_config` 里是裸
  `json.load()`，所以**所有**入口（`pipeline` / `probe` / `final` / `rules_check` …）在 config
  漏个逗号时都给一整段 traceback。现走统一守卫，提示「<path> 不是合法 JSON（…）」。
- **`--history` 指向坏 JSON → 裸 `JSONDecodeError`**（`position_reuse` 与 `build_matrix_rows` 两处）：
  上一轮只给 `--history` 补了"文件不存在"的检查，**没管"文件存在但内容不是 JSON"**。
  现两处都走 `load_json_file`。
- `smoke_path_guards` 的场景从 12 个扩到 **16 个**（补：坏 config / 坏 `--history` ×2 /
  `rules_check verify --results` 缺失），把这三条钉死。

> 前一轮的 17 个错误路径探针**漏了这一整档**：它只覆盖"文件不存在"，没覆盖"文件在、但内容不合法"。
> 教训：错误路径要按**两种维度**展开 —— 路径维度（缺 / 错）**×** 内容维度（空 / 坏 / 合法但不匹配）。

**（第三轮：复核上面这轮的改动本身，又揪出 2 处）**

- 🔴 **`pipeline probe` 被上一轮的修复弄坏了**（修 P0 时引入的新缺陷）：新增的
  `--workbook/--snapshot` 存在性校验用的是原始 `os.path.isfile()`，而 `wb` 有可能来自
  `config.excel.workbook` —— 首次使用时那里还是 `config.example.json` 的 `<...>` 占位符
  ⇒ **`pipeline probe` 直接中止**（消息还写成 `✗ …工作簿不存在：None`），而它是流程的第一步。
  现在把"意图强度"与"文件在不在"分开：
  - **命令行显式给出** → 强意图，不存在就报错（P0 那条防线不动）；
  - **来自 config 的值** → 是 `<...>` 占位符即视为**未配置**（告警后按"未提供"处理，这正是首次使用的正常状态）；
    是真实路径却不存在 → 已配置但坏了，**报错**（岗位复用也要读它，静默跳过会改结果）。
  同一个判据（`common.is_placeholder`）在 `build_matrix_rows final` 与 `pipeline probe` 两处共用；
  `probe` 是只读诊断命令，配置坏时只**明确告警**不中止（它仍要打印另外两节），`final` 才硬报错。
- **`SKILL.md` 参数速查表把全局选项写进了子命令参数**：`pipeline.py` 的 `--config` 是**全局**选项，
  表里却列在 `probe` / `run` 的参数中 ⇒ 照抄会得到 `error: unrecognized arguments`。
  已改为 `pipeline.py --config <json> probe|run` 并写明位置约束，同时加断言钉住
  「表里 pipeline 的行不得把 `--config` 列进子命令参数」+ 真跑一次确认 argparse 的形状。

### Changed

- **文档分层重构**（用户："针对所有文档的段落结构进行优化，看主文档有没有可以清理合并到子文档中去的地方"）。
  起因是一个**结构性缺陷**：`SKILL.md` 与 `references/workflow-notes.md` 在**六个主题上各写一份规则**
  （云文档接口 / 纯文本风险 / 岗位复用 / 判定规则 / 列映射 / 表格来源），措辞还不同 —— **双份真相必然漂移**。
  现在定死分工并写进 `workflow-notes.md` 开头：**`SKILL.md` 写规则与指令（唯一来源），
  `workflow-notes.md` 只写依据（为什么这么定 / 实测数字 / 踩坑经过 / 历史案例），不复制规则本体**。
  - `workflow-notes.md`：新增「与 `SKILL.md` 的分工」+ 章节索引；修**层级错配**（「云文档接口约束」
    「云文档纯文本风险」「岗位复用」原本被挂在「两种表格来源」下，其实不是它的子主题）；
    **合并自身重复的两个「岗位复用」章节**（1743 + 728 字符 → 一节）；
    「判定规则」收敛为"只留真机案例 + 指针"。
  - `SKILL.md`：「微信库结构」压掉 3 条纯机制（`Msg_<md5>` 分片 / `local_type` 码表 / `WCDB_CT` 取值
    —— 这 9 处**逐条核实过 notes 已有等价内容**，删之无损）；WPS 两节里的实测推导压成"指令一句 + 指针"；
    **删除「其他操作注意」这个杂项筐**：5 条按主题归位（问会话那条并入第 2 步、`null` 字段那条并入
    第 4 步的裁决、临时文件清理并入「表格来源」；另 2 条本就是重复，删并加指针）。
  - `README.md`：**取消「合规与隐私说明」里的忽略清单副本** —— 它已漏列 15 项（含刚加的
    `prompt_rendered.txt` / `rules_check_out/` / `validation_report.txt`，正是 G1 那几个），
    改为"以 `.gitignore` 为准"；目录树补 `.gitignore` / `LICENSE` / `test-prompts.json`。
  - `references/agent-prompt-zh.txt`：【开关渲染表】原本**插在编号列表中间**（1、2 之后直接跳 3），
    移到列表末尾；顺带修开头那句"替换为**文末**【开关渲染表】"（表当时并不在文末）。
  - **新增守卫 `smoke_doc_layering`**：`workflow-notes.md` 不得**逐字复述** `SKILL.md` 的长行
    （归一空白与标点后完全相同且 ≥30 字符）。理由：分工约定只是**写在文档里的规范**，
    没有守卫的话，下一个人复制粘贴一句规则过去就又是双份。近似重复（规则面 vs 依据面）**允许** ——
    机器判不准，硬拦会天天误报。自检两头都钉（漏报 / 误报）。
- **引用指向与歧义专项修正**（用户："看全文档文字的引用性指向是否正确、描述互相间是否有歧义、
  逻辑是否有冲突"）。为此写了一个带**白名单 + 自检**的探针（`probe_refs.py`）：
  - ⚠ **探针 v1 报的 37 条"硬问题"全是误报** —— 把运行产物（`config.json` / `payload.json` /
    `validation_report.txt`）当悬空引用、把 argparse 内建（`--help`）与**故意不存在**的参数
    （`--skip-validation`）当未知参数。v2 加白名单后**归零**。教训：扫描器不带白名单只会刷屏，没人会看。
  - **查到 1 处真泄漏（P1）**：`CHANGELOG` 里为了说明"校验报告会含带人名拼音的文件名"，
    **举了一个真机文件名的实例** —— 等于把真人姓名拼音写进仓库，违反本仓"不含任何真实姓名"的合规声明。
    这是**我自己上一轮写进去的**。已改为占位符形式，并补一条守卫（见下）。
  - 3 处引用不精确（`SKILL.md` 速览 2 处 + `workflow-notes.md` 1 处指向不存在的章节名）→ 改为可解析的目标。
  - 2 处术语歧义 → 速览加「**术语等价**」一行（`kdocs` = WPS 云文档 = 金山文档 = 那个连接器；
    "通道" = "来源"）；「表格来源」节加「**三个"起始行"参数别混**」（`--start-row-excel` /
    `--start-row` / `config.excel.start_row`）。
  - 1 处**描述与行为的落差**：提示词让子代理把裁决理由写进 `note`（字段名又叫"备注"），
    而 `final` 在落表前会**清空备注** ⇒ 容易误以为它会进台账。已在「判定规则」补一句说明 `note` 的旅程。
  - **参数速查表逐行核对**：每行参数都确实属于该行那个脚本（0 处错配）。
- **新增守卫：文档里不得出现具体的子代理产出文件名**（并入 `smoke_repo_hygiene`）。
  真机形态 `agent<字母>_<账号名>.json` 的账号名往往是人名拼音，写实例等于泄漏；
  只扫**文档**（`.md/.txt/.json`），`smoke_test` 自己的合成夹具在 `.py` 里不受影响。
  自检钉住漏报（真机形态必被命中）与误报（`agent_N.txt`、`split_for_agents.py` 不得命中）两头。
- `smoke_test.py` **同时是 CI 入口** —— 本地与 CI 跑同一条命令，避免"CI 绿、本地跑不到"
  （或反过来：本地绿、CI 红）。
- `smoke_test.py` 新增 **5 组**断言（17 → 23 项，`--full` 下 20 → 26）：输入路径守卫（12 个场景）、
  **不许静默降级（P0 回归）**、preview / `--new-rows` 列契约、Q 取值域四处一致、
  文档里的 CLI 形状与 argparse 一致（全局 `--config` 的位置），
  外加一条**「README 声明的项数 == 实际项数」**的防漂移断言（本轮靠它两次抓到自己忘了同步数字）。
- 又新增 **5 组**（23 → **28** 项，`--full` 下 26 → **31**）：**子代理产出契约**
  （四类硬拦 + 契约外字段只告警 + `--skip-validation` 必须不存在 + 干净输入仍放行 +
  `payload.json` 这类 dict 产物不得被误拦）、**快照覆盖范围 / 表内复用**
  （`coverage` 元数据、两条检查的四种组合、旧快照不误报、**云文档表内复用真的补出岗位**、
  以及一条**反向断言**：已作废的结论不许作为现行说法复活 —— 按行判，允许它作为"曾如此、已作废"留痕），
  与 **`SKILL.md` frontmatter 长度额度**（见下）。
- **修正一条作废的结论**：原文档写"云文档读历史列太贵 ⇒ 云文档不做表内复用 ⇒ 一律手写 `--history`"。
  该结论的**成本核算已被实测证伪** —— 它把"回包大小"当成了"进上下文的大小"：真机读一列
  （196 格 / 87 KB）时回包**被宿主自动落盘**、全程没进上下文，而把落盘文件直接当 `--raw` 喂 `build`
  实测成功。于是改成"**两条通道都做表内复用**（云文档前提：第 2 趟把行读全）"，
  并如实标注 `--history` 是**手写降级通道**（格式 `{"positions":{...}}` **全仓没有任何脚本会产出它**）。
  这条也解释了文档里原有的自相矛盾：第 2 趟读的关键列**本来就含** 提出人/提出人岗位
  （`sheet_snapshot.KEY_COLUMNS`），却同时声称"快照不含数据区"。
  ⚠ `[1.0.1]` 及更早的变更段里还留着"没有便宜路径 / 需 `--history` 手动喂入"的说法 ——
  那是**当时的记录**，不改历史，但**以本条为准**。
- `README.md`：「自检」节重写（说清覆盖哪些"不会报错却会改错数据"的路径 + CI 三轴各守什么）、
  加 CI 徽章；目录结构补 `.gitattributes` 与 `.github/workflows/`；
  `common.py` 的职责说明补「控制台编码 / 输入路径守卫」。
- `SKILL.md`：新增「「需求归类」（Q 列）的取值域」小节（5 个值 + 判据 + 实测分布）；
  参数速查补上那三个"通常由 pipeline 代调"的脚本里**用户自己会用**的参数
  （`--until` / `--name-filter` / `--session` / `--group` 等原先零文档）。
- `pipeline.py probe` 的 `--workbook / --snapshot` 改为**互斥**（与 `probe.py` / `position_reuse.py` 对齐；
  原先两个同时给时 snapshot 静默优先）。
- `SKILL.md` / `references/workflow-notes.md` 跟着上面那条结论一起改：第 2 趟读表那一步写明
  「**rowTo 必须读到表末**」并补上落盘实测路径（读全行 → 回包落盘 → 落盘文件直接当 `--raw`）；
  岗位复用节改写为「两条通道都做表内复用」；`--history` 在通道表 / 岗位复用节 / 两个脚本的 `--help`
  **四处**都标注「**要你自己手写** —— 仓里没有任何脚本会生成这个文件」，并给出可照抄的示例。
  另把「历史区读到 0 对」的告警从"云文档快照常见"（把配置/读取错误说成正常现象）改成**列出三种可能**，
  让人能对照 `[reuse]` 行数自己判断是"没读到"还是"本来就没有"。

> **实测**：修之前 `PYTHONIOENCODING=cp1252 python scripts/smoke_test.py` 在**第一行**输出即
> `UnicodeEncodeError`、退出码 1；修之后三种条件全部 exit 0、**28 项通过**（`--full` 31 项）——
> ① cp1252 ② 纯净解释器（无 openpyxl / zstandard）③ venv `--full`。
> 另实测「自检不污染工作区」：跑前跑后 `git status --porcelain` 条目数一致（13 → 13）。

---

## [1.0.1] - 2026-09-15

> 语义化版本判定：本条**本质是修判定表的缺陷**（漏了一种"我方无明确结果"的情形），
> 不是新增能力，故取 PATCH。新增的 `rules.ignore_vague_complaints` 只是承载该修复的开关，
> 与 skill 内"每条规则都有开关"的既有约定一致。
> 若按"新增了一个配置键"的表面口径读，也可算 MINOR —— 取决于把开关当能力还是当实现细节。

### Fixed
- **笼统抱怨被误记为需求**。真机上曾把群聊里一句「系统特别卡」记为 `bug` / 完成 ——
  对方指不出任何具体对象，我方只回了「稍等 重启系统」（运维动作，不是"已解决"）。
  根因是判定表有洞：原规则只定义了两种"我方无明确结果"的情形（**明确拒绝**、**无任何实质回复**），
  "**回复了、但回复不是已解决**"没有定义，于是落进子代理的自由裁量。
  现在：**只表达体感或情绪、指不出具体对象**的反馈，且我方未明确答复已解决 → 不记行
  （`rules.ignore_vague_complaints`，默认开）；反之给出可定位对象或量化现象
  （"搜一个箱号要等15秒""审批后卡住"）→ 仍按 `bug` 记。
- **「明确答复完成」的口径过松**：运维性动作与安抚（重启系统 / 稍后看 / 换个浏览器 / 清缓存 /
  再看看 / 我看看）不再算作完成。此前这类回复会让子代理自行判成"完成"。
- **文档把 `decrypt.py` 的参数写成了 `--db-dir`**：`--db-dir` 其实是**第三方 wcdb-key-tool 自己的参数**
  （由脚本内部透传），`decrypt.py` 的参数是 `--db-storage` —— 照文档手敲会被 argparse 拒绝。
  `SKILL.md` 与 `workflow-notes.md` 已更正，并写明两者的区别以免再混。
- **缺 `openpyxl` 时抛裸 `ImportError`**：本地表格通道的懒加载直接 `import openpyxl`，缺库会打出一整段
  traceback；而 `zstandard` 那条早已有友好提示，两处不对称。现收敛到**唯一入口**
  `common.require_openpyxl()`（4 个调用点共用），缺库时提示 `pip install openpyxl`
  并说明云文档通道不需要它。`smoke_test.py` 新增断言：用 `meta_path` 阻断器模拟"未安装"，
  锁住"必须给可执行提示而非裸异常"这个行为（不依赖真实卸载）。

### Changed
- 判定提示词新增总则：需求**必须可交付** —— 能落成一句"改什么 / 修什么 / 加什么"。
- `build_matrix_rows.OUTCOME_LABEL` 增加 `vague`（"笼统抱怨(记录)"），
  供 `ignore_vague_complaints=false` 时在预览 CSV 里与其它 `outcome` 区分。
- `smoke_test.py` 新增 `smoke_rule_consistency`：断言提示词的 `outcome` 枚举必须都在
  `OUTCOME_LABEL` 里，且四个规则开关必须在 `config.example.json` / 提示词开关表 / `SKILL.md`
  三处同步。这两类脱节都**不抛异常**（前者静默显示英文原值、后者静默渲染错提示词），只能靠断言守。
- **`SKILL.md` 按语义重整**。取消按"何时发现"组织的「真机验证过的坑」容器 ——
  11 条里混了接口契约、代码行为、流程经验、能力边界四类，且多为变更史、并与正文重复。
  机制按主题归位（写成"当前约束"而非"曾经怎样"）：接口契约 → 「表格来源」；
  正文编码 → 「微信库结构」；岗位复用两条 → 「岗位复用」；时间窗经验 → 「交互式流程」；
  依赖与 `--db-storage` → 「环境依赖」。实测与未实测（`429001` / `.ksheet` / 区域保护）分开标注。
- 正文改为**主线在前、专题在后**：何时使用 → 环境依赖 → 核心原则 → 交互式流程 →
  表格来源 → 岗位复用 → 判定规则 → 微信库结构 → 其他操作注意 → 速查。
- **`references/workflow-notes.md` 按语义重排**。标题由「工作流要点与坑位备忘」改为
  「机制说明与实测记录」，并写明它是 `SKILL.md` 的**详情层**（前者讲怎么做、后者讲为什么）。
  顺序由"主题混杂 + 发现顺序"改为「前置事实 → 流程 → 专题 → 收尾」：
  环境事实 / 解密 / 消息正文解码 → 列映射 / 两种表格来源（通道 A → 本地回填 → 通道 B →
  接口契约 → 读历史列 → 纯文本风险）→ 岗位复用 / 判定规则 → 易错点。
  原先「解密」夹在专题中间、「本地回填」被 kdocs 内容隔开 40 余行，现各自归位。
  另与 `SKILL.md` 去重重复的操作性条目，本文件只留机制与逐条实测。
- **`README.md` 的「设计与踩坑记录」再压一格**（改名「实测与可靠性」）。上一版虽已删掉 13 行坑位大表，
  但仍留着三条"最容易被忽略的结论" —— 它们与 `SKILL.md` / `workflow-notes.md` 重复，且都是
  **使用者不需要动作**的实现细节（末行判据是内部函数名、纯文本会被改写这一点已由脚本自动处理），
  对"要不要采用"这一决策没有帮助。现在只留一句可信度声明 + 指向三个文件的指针（20 行 → 5 行）。
- 参数速查表补上 `pipeline.py` 的全局 `--config`，并把表头由"与 argparse 一致"改为如实说明 ——
  该表只列常用命令；`decrypt.py` / `export_conversations.py` / `split_for_agents.py`
  通常由 `pipeline.py run` 代调。

### Removed
- `SKILL.md` 的「真机验证过的坑」「同区其他注意」「操作提醒」三节（内容已归位，或并入
  「其他操作注意」）。逐条机制推演仍在 `references/workflow-notes.md`。

---

## [1.0.0] - 2026-09-11

首个对外发布版本，提交至 <https://github.com/yfwang0731/skill-wechat-worklog-matrix> 并打 tag `v1.0.0`。
本版包含 `0.1.0` ~ `0.6.0` 的全部内容。

> **本版提供的能力**：读取微信 4.x 本地加密库并导出指定会话的聊天转录（含 ZSTD 压缩正文解压、
> 「引用回复」正文提取）；由子代理识别对方提出的系统需求并判定我方答复结果，交人工裁决后才写入台账；
> 两种台账来源（本地 `.xlsx` / WPS 云文档）；按**表头名**生成列映射、跨会话重复合并、岗位列向上复用；
> 改完代码可用 `scripts/smoke_test.py` 快速回归。
>
> **已知限制**：解密依赖第三方工具 wcdb-key-tool（不在本仓库内，需自行获取并评估合规性）；
> 云文档通道读取「历史列」的代价较高，岗位复用需用 `--history` 手动喂入；
> `.ksheet` 智能表格的写入形态**未验证**；仅在 Windows + 微信 4.1 + WPS 云文档环境下实测过。

### Added

- **`README.md` 的「安装」节**：原先只有环境依赖与运行命令，**没说文件该放到哪里** —— 补上技能目录位置
  （用户级 `~/.workbuddy/skills/`，或项目内 `<项目根>/.workbuddy/skills/`）、**目录命名要求**
  （用 `wechat-worklog-matrix`，不要保留仓库的 `skill-` 前缀、不要多套一层）与升级方式。
- `CHANGELOG.md`（本文件）。

### Changed

- `README.md` 顶部增加当前版本标识，链到对应 release。
- 本文件统一为**倒序** —— 原先 `1.0.0` 在最上是倒序、日期条目却是正叙，同一文件两种方向。

### Fixed

- **三处历史遗留的真实内容泄漏**（由全仓库脱敏扫描发现，都不在新写的文件里）：
  `references/workflow-notes.md` 举 `option_col[].texts` 例子时写了真实提出人姓名；
  `scripts/smoke_test.py` 直接拿真实客户消息原文当测试夹具；`SKILL.md` 引用客户原话举例。
  全部替换为中性表述与合成串。

> **发布校验**：仓库公开可访问；release 非 draft、非 prerelease，源码包 `zipball` / `tarball` 可用；
> 四份文档格式一致性（伪标题 / `⚠️` 入标题）0 处；脱敏扫描 40 余项敏感标识 0 命中。

---

## [0.6.0] - 2026-09-11

文档重构：理顺 `SKILL.md` 的层级、`README.md` 去重、`workflow-notes.md` 归类纠偏。

### Added

- 「**孤儿尾行按预期保留**」：只在非探针列（如项目编号）有值的尾行仍算有效数据行，本批从它**下方**追加
  （宁可留一个空行，也绝不覆盖）；要复用这类行需显式 `--start-row`。
  已写进 `SKILL.md`、`references/workflow-notes.md` 与 `common.last_data_row_ws` 的 docstring。

### Changed

- **`SKILL.md` 顶级节 15 → 11**：删掉与「真机验证过的坑」撞名的 §关键事实（踩坑记录）——
  它本是「事实」不是「坑」，独立为「微信库结构（领域事实）」；把「云文档通道专属坑」从功能节下提到坑列表；
  「岗位复用」从「第 N 步」序列里提为独立主题（它排在第 1/2/3/4 步之后，看起来像「第 5 步」，其实不是步骤）；
  三个纯速查表合并到 `## 速查` 下做 `###`。
- **统一段落层级约定并全文执行**：列表项一律以 `**标签**：` 开头；`>` 只用于警告与提醒，
  不承载规则正文；不再用加粗文本冒充标题。
- **`README.md` 瘦身 24%**：删掉与 `SKILL.md` 逐条重复的 13 行坑位大表，以及逐字重复的
  「两条表格通道对照」表；只留「设计与踩坑记录」里 3 条最反直觉的结论 + 指针。
- `references/workflow-notes.md`：把混在「判定规则」里的「岗位复用」与「云文档纯文本风险」拆成独立 `##`；
  「易错点」去重并把「末行判据双实现」补为第 4 条静默改写路径；标题里的 `⚠️` 移出。
- 去掉文档里的**对话上下文泄漏** —— 读者没在想的事（如「不需要 Release」），
  以及自我辩护、自我拔高措辞，一律删除，只留陈述事实。

---

## [0.5.0] - 2026-09-11

首次真机实战，以及 darwin 复评 4 ~ 7 驱动的连续修复。

### Fixed

- **四条「没有报错、但改动了不该动的单元格」的路径**（本项目的核心病灶，全部改为显式报错或显式 opt-in）：

  | # | 路径 | 现象 |
  |---|---|---|
  | 1 | 表头全部识别失败 | 末行判定失效，追加起始行退化成第 2 行 → 覆盖台账开头数据 |
  | 2 | `final` 的起始行兜底 | 忘传表格来源时静默从第 2 行开始写 |
  | 3 | `position_reuse` 默认扫描整表 | 回填**历史**里的空缺格（超出用户意图的越界改写） |
  | 4 | 「末数据行」判据双实现 | 探测用「全部映射列」、写入只用 3 个探针列；尾部行只在其他映射列有值时两处分歧 → 写入起始行偏小 → **覆盖尾行** |

  第 4 条已抽成**唯一判据** `common.last_data_row_ws()` 由两处共用，并用自检断言锁死
  「两处必须同值 + 表头行号必须透传」。
- 岗位复用改为「自动取追加起始行」后会**永远 0 条**（起点 = 末数据行 + 1，从那里向下扫恒为空），
  而表格尾部若还有空的「带格式行」，连提示都不会打印 —— 属「看起来跑了、其实什么也没做」。
  默认模式改为**直接报错退出**，必须显式给 `--new-rows` 或 `--start-row`。
- **导出侧一直在丢内容**（**读原始字节**定位到的根因）：微信把长文本 / 图片 / 引用 / 合并转发 /
  通话等正文**以 ZSTD 压缩**存在 `message_content` 里，早期按文本 `errors="replace"` 解码 →
  整段会话乱码、需求漏判。
  实测 8 条原乱码消息里 **4 条含真实需求正文**；其中一条是「引用回复」，正文在
  `<refermsg><content>` 里，本来可以自动识别。现在缺库或解压失败输出**明确标记**（绝不吐乱码），
  并在导出末尾统计未解码条数。
- **云文档纯文本值被表格引擎静默改写**：值一律以 `opType:"formula"` 写入，实测
  `0012` → 数值 `12`（丢前导零）；`=A1` → **被当成真公式求值**，值变成 A1 单元格的内容且不报错；
  `12345678901234567` → 丢精度；`+86` → `86`。现在**默认自动转义**（前置一个单引号，引擎把它当文本标记
  消费掉、**回读值不含引号**，实测无损），并列出命中项与原因；
  确需按公式写入时才用 `--no-escape-risky-text`。
- 表头行改取「前 5 行里**命中表头别名数最多**」的一行，非空单元格数只作次级判据 ——
  只按「非空最多」在很宽的表上会被数据行骗过（数据行往往更长更满）。
- 提出时间取**首次提出日**，不是我方答复日。
- `pipeline.py` 打印的 `final` 命令未内联表格来源、补跑提示在 D1 之后已过期 —— **照抄会崩**。
- `export_conversations.py` 的 `appmsg` 在既无标题 / 描述、也不是引用回复时会退成 `[链接/文件]`，
  纯文本型 appmsg 的需求会整条丢掉；补上外层 `<content>` / `<url>` 兜底。

> **真机结论**：四条静默改写路径全部收敛；真实台账 `final` 产物与已写入内容**逐条一致**
> （107 值 / 110 op / 分批 `[100,10]`）。
>
> **一条实测误报**：曾怀疑 `risky_reason` 的 `len(s) > 1` 限制让单字符 `=` / `-` / `+` / `@` 漏检；
> 往云文档实际写入这 4 个字符后回读**全是原样字符串**，故该限制正确（否则台账里常见的占位符 `-`
> 会被反复误报）。结论已写进 docstring 并加自检断言锁定。

---

## [0.4.0] - 2026-09-11

新增 **WPS 云文档通道**，台账来源变成两条（`config.excel.source`）。

### Added

- **WPS 云文档通道（`kdocs`）**：用户只给文档链接或名称，agent 直接读写云文档，不必把文件下载到本地。
  与本地通道**共用同一套列映射、判定规则与 payload 结构**，只换 I/O 两端。
  - 为什么必须多一层「快照」：连接器的 MCP 工具**只有 agent 能调用，Python 脚本调不到** ——
    于是读表由 agent 取数落 JSON、脚本读快照；写表由脚本产出请求体、agent 去调工具。
  - 新增 `scripts/sheet_snapshot.py`（`plan` / `build` / `inspect`）与 `scripts/to_kdocs_payload.py`。
- **消息正文解码**：识别 `WCDB_CT_message_content` 并对 ZSTD 压缩正文解压；从 `appmsg` 提取标题与
  **引用回复的原文**；系统消息 / 撤回 / 通话归一化。
- **岗位复用并入 `final`，一轮写入完成**：原先要「先写一遍、再补一遍」，两次写入之间行号可能错位；
  现在复用发生在写 `final_rows.csv` **之前**，CSV 与真正写进表格的内容永远一致。
- **`README.md` 的「设计与踩坑记录」**：只留 3 条最反直觉的结论（末行判据唯一 / 云文档无便宜历史读法 /
  纯文本被引擎静默改写），细节指向 `SKILL.md` 与 `references/`。

### Fixed

- 工作表参数名是 **`worksheet_id`**，不是 `sheetId` —— 照原样调用必失败。
- `update_range_data` 单次 `rangeData` **上限 100 条**，超出被拒（`length 110 exceeds limit 100`）；
  已自动分批。
- `decrypt` 未显式传 `--db-dir` —— 多账户机器上可能取到已停用的账号。

> 另确认：每条 `formula` 操作只能写一个值，N 个不同值必然 N 条操作，**无法靠合并收敛**
> —— 能合并的只有 `format` / `merge` / `picture` 这类区域操作。所以「6 行 × 18 列 ≈ 107 条操作」
> 是正常量级，不是脚本啰嗦。
>
> 云文档「读历史列」四条路都试过，**都没有便宜路径**：`download_file` 的 URL 需登录态
> （直接 curl 得 `403 userNotLogin`）；`get_typed_value` 会**跳过空单元格**、行列对不上；
> `get_range_data` 与 `read_file` 是同一套带字体 / 边框的格式（约 450 B/格，读两列全列约 200 KB 上下文）；
> `find_range_data` 的 `filter` 结构未公开，传错会被**静默忽略并返回整段**。

---

## [0.3.0] - 2026-09-09

**darwin-skill 8 维评审**驱动的连续修复（首评 → R1 ~ R4 → 二评 → 三评）。

### Added

- `scripts/smoke_test.py`：import 全模块 + 纯函数断言（`--full` 另含依赖项检查）。
- `probe.py` 子命令加 `--json`（纯 JSON 输出），`pipeline.py` 改走 JSON 通道、弃用哨兵字符串解析。
- `config.rules` 的判定开关接线：`references/agent-prompt-zh.txt` 增【开关渲染表】
  （关态替代句 + outcome 标签），`build_matrix_rows` 增预览标签。
- 密钥按账号目录隔离：`keys_<账号>.json`。

### Changed

- `flag_dups` 改双通道：共享单号直标（不再要求落在时间窗内）+ 无单号才做近窗相似比对，
  并修掉「同一对重复被标记两次」；复杂度从 O(n²) 降为候选对比。
- `final` 的 `--merge a:b` 增强：序号合法性校验 + **b 的空字段并入 a（不覆盖已有值）**。
- `split_for_agents` 改为按**文件大小降序贪心均衡**分包。
- `SKILL.md` 删掉「解密微信数据库」这一误导性触发词；补可选参数速查表；
  `README.md` 目录树去掉旧脚本名字面。

### Fixed

- **P0**：`export_conversations.py` 漏导入 `timedelta` → import 即 `NameError`，`run` 主链的导出必崩。
  这类错误**只在运行期暴露**，直接催生了后来「改完代码先跑 import 冒烟」的习惯。
- 表头别名匹配改为「**先精确、后子串 + 已用列校正**」—— 原先较短的别名会抢走更长的表头
  （例如「项目负责人」被「项目」抢去挂到「项目名称」上）。
- 提出人剥离岗位词时加**词边界**，修掉把提出人剥空成 `(未知)`。
- `serial()` 容错多种日期写法；单号识别排除 `YYYYMMDD` 形式的日期串；导出文件名加哈希防覆盖。
- `probe` 遇到旧版 `.xls` 给出明确报错（原先裸崩），`position_reuse` 兼容 `.xlsm`，两者行为对齐。
- `probe --sheet` 无匹配时友好报错；`config.example.json` 补 `post_words` 与映射说明。
- 提示词必填占位符 `{{服务对象}}` / `{{业务系统描述}}` 原本无 config 来源 →
  新增 `people.service_object` 与 `excel.business_system_description`；`{{对方关键字}}` 补入必填清单。

> **评测轨迹**（独立子代理盲评 + 代码实跑）：70.2 → 76.0 → 79.5 → 81.2 → 84.5
> → **独立复查下调至 74.7** → 79.0 → 90.5。两次下调都来自「复查推翻自评」，
> 完整轨迹见文末「附：独立评测记录」。

---

## [0.2.0] - 2026-09-09

从「一次性脚本」变成**配置驱动的通用流水线**，并完成全量脱敏。

### Added

- `scripts/common.py`：微信账号目录 / 解密工具路径自动识别、日期序列号转换、列号换算、追加起始行计算。
- `scripts/decrypt.py`：封装 wcdb-key-tool 的 extract / decrypt。
- `scripts/split_for_agents.py`：转录按文件大小均衡分包给并行子代理。
- `scripts/probe.py` + `config.example.json`：**探测微信账户 / 可选会话 / 表格表头 → 列映射**。
- `pipeline.py` 编排入口：`probe`（探测 → 一次性确认）/ `run`（解密 → 导出 → 分包）。
- `LICENSE`（MIT）。

### Changed

- **改造为通用配置驱动**：`build_matrix_rows.py` 按 `column_mapping` 输出（不再写死 A~AA）；
  `export_conversations.py` 按 config 选账户 / 会话 / 时间窗；岗位复用按表头映射定位列。
- **全量脱敏**：代码、文档、提示词模板全部占位符化（无姓名 / 微信号 / 账号目录 / 客户名 /
  项目号 / 群 ID），共 36 处替换；路径统一为 `%USERPROFILE%`。
- `n_reuse_plan.py` → **`position_reuse.py`**（用 `git mv` 保留历史），
  并同步 `SKILL.md` / `README.md` / `references/` / `pipeline.py` 提示 / `smoke_test.py`。
- **统一 run 链路的路径基准**：`pipeline.cmd_run` 改为从 config 读路径、解析成绝对路径后显式传给子进程 ——
  原先依赖子进程的工作目录，产物落错位置，且打印路径与实际不符。
- `SKILL.md` 增加「环境依赖」与快速开始；子表名改为按关键字**动态匹配**（不再写死年份）；
  修正触发场景里的误导表述（原先把「导出聊天记录」列为使用场景，而它只是中间步骤）。
- 发布到 GitHub；修 Git 凭据助手反复弹窗（根因是 system 级 `credential.helper=helper-selector`
  在每次操作时最先触发选择框，三层配置统一指向凭据管理器）；
  移除本地历史快照目录改用 git 提交管理，此后不再生成文件级快照。

---

## [0.1.0] - 2026-09-08

从「微信聊天记录 → 需求跟踪矩阵」这一实际任务中固化出的经验，建成本 skill。**首个可用版本。**

### Added

- `SKILL.md`、`scripts/export_conversations.py`、`scripts/build_matrix_rows.py`、
  `scripts/n_reuse_plan.py`、`references/agent-prompt-zh.txt`、`references/workflow-notes.md`。
- **岗位列向上复用**：按行序向上找同一提出人最近的岗位值填补空缺，**只填空缺、不覆盖更具体值**。
- 摸清微信 4.x 库结构（后固化为「微信库结构（领域事实）」一节）：会话分表 `Msg_<md5(wxid)>`、
  发送者数字 id 是**分片内**的 `Name2Id.rowid`、消息库分片 `message_1/2/3.db` 按时间序、
  `create_time` 秒级、`local_type` 取低 32 位为类型。

### Fixed

- 文件被宿主或外部程序占用时保存报 `Export file is occupied` → `save_file` 到临时路径 →
  `close_file(force)` → 用二进制覆写原文件（写共享通常允许，`delete` / `rename` 会被锁拒绝）。

> **背景**：微信 4.x 本地库是 SQLCipher 加密的，且官方没有可读的导出通道。逐一排除的路线 ——
> 手机端「导出到电脑」是**加密迁移**（落盘仍是加密库，无可读中间产物）；
> 桌面端 UI 自动化在本环境不可行（消息区用自研渲染引擎，UIA 读不出文本；Ctrl+A 无效；右键不弹菜单）；
> 用户手动多选复制**单次上限 100 条**；网页版微信该账号被禁登录；现成的开源导出工具或已停止维护、
> 或仅支持更低的微信版本（需降级，侵入过大）。
> 最终选定第三方工具 **wcdb-key-tool**（Windows 4.1+ 可用、无需降级微信、只读扫描进程内存取密钥）：
> 一次提取命中 **22/27** 个库（4.7 秒），解密全部成功（失败的 5 个均为历史备份副本）。
>
> **首次实战**：26 个会话 / 3046 条消息 → 6 个并行子代理识别 → 143 条候选需求 →
> 人工裁决（合并跨会话重复、保留答疑类）→ **139 行**写入台账。
>
> **同期确立两条约定**（此后一直执行）：① **单元格只写业务结果**，备注等列不得出现判定依据 /
> 分析过程（当时清空了 32 处这类内容）；② 本地编辑引擎会把 `.xls` 内容写成 `.xlsx`，
> **保存后必须确认 / 改名**。

---

## 附：独立评测记录

本项目全程用 `darwin-skill` 的 8 维 rubric 做**独立**评测 —— 结构维度由主评审打分，
效果维度派**独立子代理盲评**（不告知主评分数），再归并仲裁。

**分数不是单调上升的** —— 两次独立复查都下调了上一轮的分数：

| 时间 | 被评对象 | 分数 | 关键发现 |
|---|---|---|---|
| 09-09 | 初始状态 | **70.2** | 基线：P0（`timedelta` 未导入必崩）+ P1×6 |
| 09-09 | 修复 R1→R4 | → **84.5** | 三轮修复，逐轮加分 |
| 09-09 | **独立复查** | 84.5 → **74.7** ⬇ | 复查推翻自评：未捕获异常、改名残留、维度 8 自评虚高 |
| 09-09 | 二评 | → **79.0** | 又暴露「假开关」：4 个 config 开关实际无脚本读取 |
| 09-09 | 三评 | 86.5 → **90.5** | 一处维度争议经用户裁决修正 |
| 09-11 | 复评4（新增云文档通道后） | **80.0** | 新功能引入新缺口 |
| 09-11 | 复评5 | → **86.0** | 按真机结果重整后 |
| 09-11 | 复评6 | → **87.0** | 修复复评5 的 D1 / D3 |
| 09-11 | **复评7** | 87.0 → **79.0** ⬇ | 查出潜伏高危「末数据行判据双实现」 |
| 09-11 | 复评7 修复后 | → **90.0** | 判据统一 + 独立复核通过 |
| 09-18 | 复评8（基线） | **88.5** | 独立复查**下调**：结构维 66.0 / 75，指出「整体架构」缺导航层 |
| 09-18 | 加「30 秒速览」 | → **90.0** | 整体架构 8 → 9；效果维由独立子代理重测，未退化 |
| 09-18 | 三步走（结构 / 引用 / 全流程） | → **90.8** | 分层定死 + 消术语歧义；效果维独立复测，改后臂最完整 |
| 09-18 | 第四轮（文档守卫固化） | → **93.3** | 见下：效果维**连续三轮**独立复测（可执行性 7 → 8 → 9） |

> **09-18 第四轮的效果维是怎么评的**：按 darwin 的"不能自己改自己评"，效果维由**独立子代理
> 跑了三次**，每次都只给同一批测试 prompt、不告知上一轮分数。三轮的可执行性自评分别为
> **7 → 8 → 9**，且每一轮指出的缺口都在下一轮前落地（解密时机、`kdocs.link`、
> 会话清单从哪个目录来、`decrypt.py --output` 默认值是否与 probe 查找位置一致）。
> 分数仍取**收敛后的** 9.5 —— 留 0.5 是因为最后一轮仍提了一条"软缝隙"（本轮已补），
> 而**补完没有再跑第四次独立复测**，这一处未经独立验证。

## 设计上的不变量（勿破坏）

1. **不写死任何具体项目 / 人员 / 客户** —— 一切从 `config.json` 读。
2. **列位置靠表头名识别**，不写死列字母（换模板也能用）。
3. **先探测 → 一次性确认 → 再执行**，不逐个追问、不替用户假设。
4. **单元格只写业务结果**，不写判定依据 / 分析过程。
5. **绝不静默改写已有数据** —— 拿不到「追加起始行」就报错退出，不兜底、不猜。
6. **同一判定只能有一个实现** —— 探测与写入都不例外（这是第 4 条静默改写路径换来的教训）。
7. **只记可交付的需求** —— 笼统抱怨与体感反馈不是需求；台账是交付物，不是意见箱。
8. **忽略名单这类 glob 必须"两头都钉"，断言不许只钉一种形态** —— 过宽会吞掉本该随 skill
   分发的文件（CI 会响），过窄会放走含聊天原文的产物（**静默，更严重**）。两种都真发生过：
   单形态断言等于单点失效 —— 它钉住的永远只是自己恰好想到的那一种写法。

[1.1.0]: https://github.com/yfwang0731/skill-wechat-worklog-matrix/releases/tag/v1.1.0
[1.0.1]: https://github.com/yfwang0731/skill-wechat-worklog-matrix/releases/tag/v1.0.1
[1.0.0]: https://github.com/yfwang0731/skill-wechat-worklog-matrix/releases/tag/v1.0.0
