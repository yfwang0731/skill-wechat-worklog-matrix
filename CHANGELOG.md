# 更新日志

本文件记录 `wechat-worklog-matrix` 的完整开发过程。

本项目**不使用语义化版本号**——它是一个由真实使用场景驱动、随实战持续打磨的技能，没有「发布版本」的概念，
所以按**日期**分组，不编造 `1.0.0`。

内容由开发过程中的工作日志与 git 提交历史整理而成。**所有条目均已脱敏**：不含任何真实姓名、
微信号、账号目录、客户名、项目编号或群标识。

被反复验证过的设计约束汇总在文末「设计上的不变量」；质量演进见「附：独立评测记录」。

---

## 2026-09-08 — 缘起：把一条「本来走不通」的路走通，并抽出技能

### 背景

需求：把指定微信群/联系人里的聊天记录，整理成**需求跟踪矩阵**台账（对方在微信里提的系统需求，
由运维方登记入表）。

障碍：微信 4.x 本地库是 SQLCipher 加密的，且**官方没有可读的导出通道**。逐一排除的路线：

| 路线 | 结论 |
|---|---|
| 手机端「导出到电脑」 | 是**加密迁移**，落盘仍是加密库，无可读中间产物（实测确认） |
| 桌面端 UI 自动化（模拟人工选中复制） | **本环境不可行**：微信 4.x 消息区用自研渲染引擎，UIA 读不出文本；Ctrl+A 在消息区无效；右键消息气泡不弹上下文菜单 |
| 用户手动多选复制 | 单次多选**上限 100 条**，数据量级下不可行 |
| 网页版微信 | 该账号被禁登录 |
| 现成的开源导出工具 | 主流项目已因故停止维护，或仅支持更低的微信版本（需降级，侵入过大） |

### 落地

- 选定第三方工具 **wcdb-key-tool**（Windows 4.1+ 可用、无需降级微信、只读扫描进程内存取密钥）：
  一次提取命中 **22/27** 个库（4.7 秒），解密全部成功（失败的 5 个均为历史备份副本，无影响）。
- 摸清微信 4.x 库结构，后固化为「微信库结构（领域事实）」一节：会话分表 `Msg_<md5(wxid)>`、
  发送者数字 id 是**分片内**的 `Name2Id.rowid`、消息库分片 `message_1/2/3.db` 按时间序、
  `create_time` 秒级、`local_type` 取低 32 位为类型。
- 首次实战：**26 个会话 / 3046 条消息** → 6 个并行子代理识别 → 143 条候选需求 →
  人工裁决（合并跨会话重复、保留答疑类）→ **139 行**写入台账。

### 抽出技能

把这条链路固化为用户级技能 `wechat-worklog-matrix`：`SKILL.md` +
`scripts/export_conversations.py` / `build_matrix_rows.py` / `n_reuse_plan.py` +
`references/agent-prompt-zh.txt` / `workflow-notes.md`。

同期新增**岗位列向上复用**：按行序向上找同一提出人最近的岗位值填补空缺，
**只填空缺、不覆盖更具体值**。

### 确立两条至今一直执行的约定

1. **单元格只写业务结果** —— 备注等列不得出现判定依据/分析过程（当时清空了 32 处这类内容）。
2. 本地编辑引擎会把 `.xls` 内容写成 `.xlsx`，**保存后必须确认/改名为 `.xlsx`**。

### 踩到的坑

文件被宿主或外部程序占用时保存报 `Export file is occupied` → 兜底链路：
`save_file` 到临时路径 → `close_file(force)` → 用二进制覆写原文件
（写共享通常允许，`delete`/`rename` 会被锁拒绝）。

## 2026-09-09 — 从「一次性脚本」变成「配置驱动的通用流水线」

### 新增

- `common.py` —— 微信账号目录 / 解密工具路径自动识别、日期序列号转换、列号换算、追加起始行计算。
- `decrypt.py` —— 封装 wcdb-key-tool 的 extract / decrypt；**密钥按账号目录隔离**，多账号不串。
- `split_for_agents.py` —— 转录按**文件大小降序贪心均衡**分包，交给并行子代理。
- `pipeline.py` —— 编排入口：`probe`（探测 → 一次性确认）/ `run`（解密 → 导出 → 分包）。
- `probe.py` + `config.example.json` —— **探测微信账户 / 可选会话 / 表格表头 → 列映射**。
- `smoke_test.py` —— 自检：import 全模块 + 纯函数断言（`--full` 另含依赖项检查）。
- `LICENSE`（MIT）。

### 变更

- **改造为通用配置驱动**：`build_matrix_rows.py` 按 `column_mapping` 输出（不再写死 A~AA）；
  `export_conversations.py` 按 config 选账户/会话/时间窗；岗位复用按表头映射定位列。
- **全量脱敏**：代码、文档、提示词模板全部占位符化（无姓名 / 微信号 / 账号目录 / 客户名 / 项目号 / 群 ID），
  共 36 处替换；路径统一为 `%USERPROFILE%`。
- `n_reuse_plan.py` → **`position_reuse.py`**（用 `git mv` 保留历史）。
- **统一 run 链路的路径基准**：`pipeline.cmd_run` 改为从 config 读路径、解析成绝对路径后显式传给子进程 ——
  原先依赖子进程的工作目录，产物落错位置，且打印路径与实际不符。
- `flag_dups` 改为双通道：共享单号直接标记（不再要求落在时间窗内）+ 无单号才做近窗相似比对，
  并修掉「同一对重复被标记两次」。
- `final` 的 `--merge a:b` 增强：序号合法性校验 + **b 的空字段并入 a（不覆盖已有值）**。

### 修复

- **P0**：`export_conversations.py` 漏导入 `timedelta` → import 即 `NameError`，`run` 主链的导出必崩。
  ⚠️ 这类错误**只在运行期暴露**，这直接催生了后来「改完代码先跑 import 冒烟」的习惯。
- `probe` 的表头别名匹配改为「**先精确、后子串 + 已用列校正**」—— 原先较短的别名会抢走更长的表头
  （例如「项目负责人」被「项目」抢去挂到「项目名称」上）。
- 提出人剥离岗位词时加**词边界**，修掉把提出人剥空成 `(未知)`。
- `serial()` 容错多种日期写法；单号识别排除 `YYYYMMDD` 形式的日期串；导出文件名加哈希防覆盖。
- `probe` 遇到旧版 `.xls` 优雅报错（原先裸崩），并与岗位复用脚本的行为对齐。

### 文档

- `SKILL.md` 增加「环境依赖」与快速开始；子表名改为**按关键字动态匹配**（不再写死年份）。
- 修正提示词触发场景里的误导表述（原先把「导出聊天记录」列为使用场景，而它只是中间步骤）。

### 工程

- **发布到 GitHub**：`git init` + `.gitignore`（排除 `__pycache__`、运行产物、`*.db`、密钥缓存、`config.json`）。
- 修 Git 凭据弹窗：根因是 system 级 gitconfig 的 `credential.helper=helper-selector` 每次操作都弹选择框 →
  三层配置统一指向凭据管理器。
- 移除本地历史快照目录采用 git 提交管理，此后不再生成文件级快照。

## 2026-09-11 — 真机实战：新增 WPS 云文档通道，并连续修掉一批「静默改错数据」

### 新增

- **WPS 云文档通道**（`excel.source=kdocs`）—— 用户只给文档链接或文件名，agent 直接读写云文档，
  不必把文件下载到本地。与本地通道**共用同一套列映射、判定规则与 payload 结构**，只换 I/O 两端。
  - 为什么必须多一层「快照」：连接器的 MCP 工具**只有 agent 能调用，Python 脚本调不到**。
    于是读表由 agent 取数落 JSON、脚本读快照；写表由脚本产出请求体、agent 去调工具。
  - 新增 `sheet_snapshot.py`（`plan` / `build` / `inspect`）与 `to_kdocs_payload.py`。
- **消息正文解码** —— 识别 `WCDB_CT_message_content` 并对 ZSTD 压缩正文解压；
  从 `appmsg` 提取标题与**引用回复的原文**；系统消息 / 撤回 / 通话归一化。
- **岗位复用并入 `final`，一轮写入完成** —— 原先要「先写一遍、再补一遍」，
  两次写入之间行号可能错位；现在复用发生在写 CSV 之前，CSV 与真正写进表格的内容永远一致。

### 修复 —— 只有真机才会暴露的接口问题

| 问题 | 后果 |
|---|---|
| 工作表参数名是 **`worksheet_id`**，不是 `sheetId` | 照原样调用必失败 |
| `update_range_data` 单次 `rangeData` **上限 100 条** | 超出被拒（`length 110 exceeds limit 100`）；已自动分批 |
| `decrypt` 未显式传 `--db-dir` | 多账户机器上可能取到已停用的账号 |

> 另外确认：每条 `formula` 操作只能写一个值，N 个不同值必然 N 条操作，**无法靠合并收敛** ——
> 能合并的只有 `format` / `merge` / `picture` 这类区域操作。所以「6 行 × 18 列 ≈ 107 条操作」
> 是正常量级，不是脚本啰嗦。

### 修复 —— 「静默改写数据」类（本项目的核心病灶）

这是本项目历史上最危险的一类问题：**没有报错，但改动了不该动的单元格**。
共发现 **四条**路径，现已全部改为显式报错或显式 opt-in：

| # | 路径 | 现象 |
|---|---|---|
| 1 | 表头全部识别失败 | 末行判定失效，追加起始行退化成第 2 行 → 覆盖台账开头数据 |
| 2 | `final` 的起始行兜底 | 忘传表格来源时静默从第 2 行开始写 |
| 3 | `position_reuse` 默认扫描整表 | 回填**历史**里的空缺格（超出用户意图的越界改写） |
| 4 | 「末数据行」判据双实现 | 探测用「全部映射列」、写入只用 3 个探针列；尾部行只在其他映射列有值时两处分歧 → 写入起始行偏小 → **覆盖尾行** |

第 4 条是最后一次独立评审查出的**潜伏高危**：已抽成**唯一判据** `common.last_data_row_ws()`
由两处共用，并用自检断言锁死「两处必须同值 + 表头行号必须透传」。

同类还有一条：岗位复用改为「自动取追加起始行」后会**永远 0 条**（起点 = 末数据行 + 1，
从那里向下扫恒为空），而表格尾部若还有空的「带格式行」，连提示都不会打印 ——
属于「看起来跑了、其实什么也没做」。现在默认模式**直接报错退出**，
必须显式给 `--new-rows` 或 `--start-row`。

### 修复 —— 云文档写入的纯文本风险（真机实测）

所有值都以 `opType:"formula"` 写入，于是纯文本会被表格引擎**静默改写**（回读实测）：

| 写入值 | 实际存成 |
|---|---|
| `0012` | 数值 `12`（丢前导零） |
| `=A1` | **被当成真公式求值**，值变成 A1 单元格的内容（不报错） |
| `12345678901234567` | 丢精度 |
| `+86` | 数值 `86` |

→ 现**默认自动转义**（前置一个单引号，引擎把它当文本标记消费掉、**回读值不含引号**，实测无损），
并在输出里列出命中项与原因；确需按公式写入时才用 `--no-escape-risky-text`。

### 修复 —— 导出侧一直在丢内容（本轮最有价值的发现）

**读原始字节**定位到的根因：微信把长文本 / 图片 / 引用 / 合并转发 / 通话等正文
**以 ZSTD 压缩**存在 `message_content` 里，早期按文本 `errors="replace"` 解码 → 整段会话乱码、需求漏判。

- 真机实测：8 条原乱码消息里 **4 条含真实需求正文**。其中一条是「引用回复」，
  正文在 `<refermsg><content>` 里 —— **本来可以自动识别，当时却只能去问用户**。
- 现在缺库或解压失败时输出**明确标记**（绝不吐乱码），并在导出末尾统计未解码条数。

### 修复 —— 流程与判定细节

- 表头行取「前 5 行里**命中表头别名数最多**」的一行，非空单元格数只作次级判据 ——
  只按「非空最多」在很宽的表上会被数据行骗过（数据行往往更长更满）。
- 提出时间取**首次提出日**，不是我方答复日。
- 补齐云文档 `--history` 手动喂岗位、风险值转义开关等配套参数。

### 文档

- **`SKILL.md` 重整层级**：顶级节 15 → 11 —— 消除两个名字都像「踩坑记录」的重复节、
  把「云文档通道专属坑」从功能节下提到坑列表、把「岗位复用」从「第 N 步」序列里提为独立主题、
  三个速查表合并。
- 统一**段落层级**约定：列表项一律以 `**标签**：` 开头；`>` 只用于警告与提醒、不承载规则正文；
  不再用加粗文本冒充标题。
- **`README.md` 瘦身 24%**：删掉与 `SKILL.md` 逐条重复的坑位大表，以及逐字重复的通道对照表。
- `references/workflow-notes.md` 承接「为什么这样 + 完整推演」，与 `SKILL.md` 形成**摘要 / 详情**两层。

---

## 附：独立评测记录

本项目全程用 `darwin-skill` 的 8 维 rubric 做**独立**评测 ——
结构维度由主评审打分，效果维度派**独立子代理盲评**（不告知主评分数），再归并仲裁。

值得强调的是：**分数不是单调上升的**。两次独立复查都把自己上一轮的分数**下调**了，
这恰恰是评测在起作用的证据：

| 时间 | 被评对象 | 分数 | 关键发现 |
|---|---|---|---|
| 09-09 | 初始状态 | **70.2** | 基线：P0（`timedelta` 未导入必崩）+ P1×6 |
| 09-09 | 修复 R1→R4 | → **84.5** | 三轮修复，逐轮加分 |
| 09-09 | **独立复查** | 84.5 → **74.7** ⬇ | 复查推翻自评：未捕获异常、改名残留、维度 8 自评虚高 |
| 09-09 | 二评 | → **79.0** | 又暴露「假开关」：4 个 config 开关实际无脚本读取 |
| 09-09 | 三评 | 86.5 → **90.5** | 一处维度争议经用户裁决修正 |
| 09-11 | 复评4（新增云文档通道后） | **80.0** | 新功能引入新缺口 |
| 09-11 | 复评5 | → **86.0** | 按真机结果重整后 |
| 09-11 | 复评6 | → **87.0** | 修复复评5 的 D1/D3 |
| 09-11 | **复评7** | 87.0 → **79.0** ⬇ | 查出潜伏高危「末数据行判据双实现」 |
| 09-11 | 复评7 修复后 | → **90.0** | 判据统一 + 独立复核通过 |

---

## 设计上的不变量（勿破坏）

1. **不写死任何具体项目 / 人员 / 客户** —— 一切从 `config.json` 读。
2. **列位置靠表头名识别**，不写死列字母（换模板也能用）。
3. **先探测 → 一次性确认 → 再执行**，不逐个追问、不替用户假设。
4. **单元格只写业务结果**，不写判定依据 / 分析过程。
5. **绝不静默改写已有数据** —— 拿不到「追加起始行」就报错退出，不兜底、不猜。
6. **同一判定只能有一个实现** —— 探测与写入都不例外（这是第 4 条静默改写路径换来的教训）。
